{
  if (variableDescriptor.isVar() && variableDescriptor instanceof LocalVariableDescriptor) {
    if (BindingContextUtils.isVarCapturedInClosure(bindingContext,variableDescriptor))     return false;
    return true;
  }
  return false;
}
