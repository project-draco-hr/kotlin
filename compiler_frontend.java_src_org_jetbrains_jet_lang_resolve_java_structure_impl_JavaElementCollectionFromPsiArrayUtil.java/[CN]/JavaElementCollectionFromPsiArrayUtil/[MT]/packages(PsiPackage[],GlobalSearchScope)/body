{
  return convert(packages,new Factory<PsiPackage,JavaPackage>(){
    @NotNull @Override public JavaPackage create(    @NotNull PsiPackage aPackage){
      return new JavaPackageImpl(aPackage,scope);
    }
  }
);
}
