{
  if (context.isEcma5()) {
    return Arrays.asList(initializer == null ? JsLiteral.NULL : initializer,new JsDocComment(JsAstUtils.LENDS_JS_DOC_TAG,context.getQualifiedReference(descriptor)),members);
  }
 else {
    return Collections.<JsExpression>singletonList(members);
  }
}
