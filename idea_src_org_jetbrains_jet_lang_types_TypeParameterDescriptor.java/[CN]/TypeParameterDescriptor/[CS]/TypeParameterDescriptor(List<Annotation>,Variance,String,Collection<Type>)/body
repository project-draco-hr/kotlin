{
  super(annotations,name);
  this.variance=variance;
  this.upperBounds=upperBounds;
  this.typeConstructor=new TypeConstructor(annotations,name,Collections.<TypeParameterDescriptor>emptyList(),upperBounds);
}
