{
  super(psiElement,attributes,name);
  this.variance=variance;
  this.upperBounds=upperBounds;
  this.typeConstructor=new TypeConstructor(attributes,false,"&" + name,Collections.<TypeParameterDescriptor>emptyList(),upperBounds);
}
