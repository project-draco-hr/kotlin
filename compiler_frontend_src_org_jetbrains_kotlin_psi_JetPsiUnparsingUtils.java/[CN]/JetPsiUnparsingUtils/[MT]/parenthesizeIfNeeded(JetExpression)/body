{
  String text=JetPsiUtil.getText(expression);
  return (expression instanceof JetParenthesizedExpression || expression instanceof JetConstantExpression || expression instanceof JetSimpleNameExpression|| expression instanceof JetDotQualifiedExpression) ? text : "(" + text + ")";
}
