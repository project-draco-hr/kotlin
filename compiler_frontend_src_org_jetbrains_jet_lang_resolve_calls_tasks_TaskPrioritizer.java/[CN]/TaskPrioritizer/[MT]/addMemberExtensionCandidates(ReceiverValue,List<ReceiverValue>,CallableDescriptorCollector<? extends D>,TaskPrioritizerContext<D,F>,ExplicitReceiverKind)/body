{
  Collection<? extends D> memberExtensions=callableDescriptorCollector.getNonMembersByName(thisObject.getType().getMemberScope(),c.name,c.context.trace);
  List<ReceiverValue> thisObjects=AutoCastUtils.getAutoCastVariants(thisObject,c.context);
  c.result.addCandidates(convertWithReceivers(memberExtensions,thisObjects,receiverParameters,receiverKind));
}
