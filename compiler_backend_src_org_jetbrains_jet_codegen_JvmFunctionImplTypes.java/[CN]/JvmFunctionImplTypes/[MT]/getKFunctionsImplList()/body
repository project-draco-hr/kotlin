{
  if (kFunctionsList == null) {
    MutablePackageFragmentDescriptor reflect=new MutablePackageFragmentDescriptor(DescriptorUtils.getContainingModule(functionImpl),new FqName("kotlin.reflect"));
    ImmutableList.Builder<KFunctions> builder=ImmutableList.builder();
    for (int i=0; i < KotlinBuiltIns.FUNCTION_TRAIT_COUNT; i++) {
      builder.add(new KFunctions(createKFunctionImpl(reflect,"KFunctionImpl" + i,reflectionTypes.getKFunction(i)),createKFunctionImpl(reflect,"KMemberFunctionImpl" + i,reflectionTypes.getKMemberFunction(i)),createKFunctionImpl(reflect,"KExtensionFunctionImpl" + i,reflectionTypes.getKExtensionFunction(i))));
    }
    kFunctionsList=builder.build();
  }
  return kFunctionsList;
}
