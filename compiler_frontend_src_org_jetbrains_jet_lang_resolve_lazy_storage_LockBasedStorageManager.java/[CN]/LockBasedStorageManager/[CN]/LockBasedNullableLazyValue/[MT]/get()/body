{
  if (computed) {
    return value;
  }
synchronized (lock) {
    if (!computed) {
      T _value=computable.compute();
      value=_value;
      computed=true;
      postCompute(_value);
      return _value;
    }
    return value;
  }
}
