{
  if (element instanceof JetClassOrObject && KotlinMoveFilesOrDirectoriesHandler.isMovableClass((JetClassOrObject)element)) {
    return element.getContainingFile();
  }
 else {
    return element;
  }
}
