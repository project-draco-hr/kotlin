{
  return inner(new AbstractThisAliasProvider(){
    @Nullable @Override public JsNameRef get(    @NotNull DeclarationDescriptor descriptor){
      return correspondingDescriptor == normalize(descriptor) ? alias.makeRef() : null;
    }
  }
);
}
