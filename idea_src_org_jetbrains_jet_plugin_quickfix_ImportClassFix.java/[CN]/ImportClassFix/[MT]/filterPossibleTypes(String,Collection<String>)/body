{
  return Collections2.filter(variants,new Predicate<String>(){
    @Override public boolean apply(    @Nullable String fqName){
      return fqName != null && (fqName.endsWith('.' + typeName) || fqName.equals(typeName));
    }
  }
);
}
