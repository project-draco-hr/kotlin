{
  JetTypeReference extendsBound=typeParameter.getExtendsBound();
  return new TypeParameterDescriptor(toAttributes(typeParameter.getModifierList()),typeParameter.getVariance(),typeParameter.getName(),extendsBound == null ? Collections.<Type>singleton(JetStandardClasses.getAnyType()) : Collections.singleton(toType(extendsBound)));
}
