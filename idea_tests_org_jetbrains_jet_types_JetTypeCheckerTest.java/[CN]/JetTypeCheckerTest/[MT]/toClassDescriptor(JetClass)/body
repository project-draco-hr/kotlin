{
  return new ClassDescriptor(toAttributes(classElement.getModifierList()),classElement.getName(),toTypeParameters(classElement.getTypeParameters()),toTypes(classElement.getDelegationSpecifiers()));
}
