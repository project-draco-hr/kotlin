{
  if (typeConstructor == null) {
    this.typeConstructor=new TypeConstructorImpl(this,Collections.<Annotation>emptyList(),!open,getName(),typeParameters,supertypes);
  }
  return typeConstructor;
}
