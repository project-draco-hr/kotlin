{
  int size=memoizedSerializedSize;
  if (size != -1)   return size;
  size=0;
{
    int dataSize=0;
    for (int i=0; i < names_.size(); i++) {
      dataSize+=com.google.protobuf.CodedOutputStream.computeBytesSizeNoTag(names_.getByteString(i));
    }
    size+=dataSize;
    size+=1 * getNamesList().size();
  }
  memoizedSerializedSize=size;
  return size;
}
