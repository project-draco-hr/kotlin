{
  if (!hasConstructor()) {
    return false;
  }
  if (!getConstructor().isInitialized()) {
    return false;
  }
  for (int i=0; i < getArgumentsCount(); i++) {
    if (!getArguments(i).isInitialized()) {
      return false;
    }
  }
  return true;
}
