{
  int size=memoizedSerializedSize;
  if (size != -1)   return size;
  size=0;
  for (int i=0; i < member_.size(); i++) {
    size+=com.google.protobuf.CodedOutputStream.computeMessageSize(1,member_.get(i));
  }
{
    int dataSize=0;
    for (int i=0; i < className_.size(); i++) {
      dataSize+=com.google.protobuf.CodedOutputStream.computeInt32SizeNoTag(className_.get(i));
    }
    size+=dataSize;
    size+=1 * getClassNameList().size();
  }
  memoizedSerializedSize=size;
  return size;
}
