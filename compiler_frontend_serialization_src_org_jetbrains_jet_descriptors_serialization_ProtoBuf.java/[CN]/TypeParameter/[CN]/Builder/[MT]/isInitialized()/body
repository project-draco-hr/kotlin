{
  if (!hasId()) {
    return false;
  }
  if (!hasName()) {
    return false;
  }
  for (int i=0; i < getUpperBoundsCount(); i++) {
    if (!getUpperBounds(i).isInitialized()) {
      return false;
    }
  }
  return true;
}
