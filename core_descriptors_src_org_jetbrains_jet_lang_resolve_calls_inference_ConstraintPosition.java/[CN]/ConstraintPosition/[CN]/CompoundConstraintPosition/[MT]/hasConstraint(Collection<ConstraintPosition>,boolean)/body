{
  return ContainerUtil.exists(positions,new Condition<ConstraintPosition>(){
    @Override public boolean value(    ConstraintPosition constraintPosition){
      return constraintPosition.isStrong() == strong;
    }
  }
);
}
