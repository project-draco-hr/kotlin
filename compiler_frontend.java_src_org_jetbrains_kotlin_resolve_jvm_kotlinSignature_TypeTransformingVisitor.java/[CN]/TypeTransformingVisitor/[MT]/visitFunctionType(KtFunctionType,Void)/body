{
  KotlinBuiltIns builtIns=JvmPlatform.INSTANCE$.getBuiltIns();
  return visitCommonType(type.getReceiverTypeReference() == null ? builtIns.getFunction(type.getParameters().size()) : builtIns.getExtensionFunction(type.getParameters().size()),type);
}
