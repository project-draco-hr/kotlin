{
  DeclarationDescriptor operationDescriptor=getOperationDescriptor(expression,context);
  return (operationDescriptor.getName().equals("compareTo") && (context.intrinsics().hasDescriptor(operationDescriptor)));
}
