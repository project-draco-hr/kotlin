{
  while (true) {
    ConcurrentMap<Key,Object> map=myUserMap;
    if (map != null)     return map;
    map=createDataMap(2);
    boolean updated=true;
    if (updated) {
      return map;
    }
  }
}
