{
  if (getter == null) {
    return null;
  }
  return new PropertyGetterDescriptor(propertyDescriptor,annotationResolver.resolveAnnotations(getter.getMember().getPsiMember()),Modality.OPEN,visibility,true,false,kind);
}
