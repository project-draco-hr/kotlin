{
  ResolvedCall<?> resolvedCall=getResolvedCallForCallExpression(context().bindingContext(),expression);
  return CallBuilder.build(context()).receiver(receiver).callee(getCalleeExpression(resolvedCall)).args(translateArguments()).resolvedCall(resolvedCall).type(callType).translate();
}
