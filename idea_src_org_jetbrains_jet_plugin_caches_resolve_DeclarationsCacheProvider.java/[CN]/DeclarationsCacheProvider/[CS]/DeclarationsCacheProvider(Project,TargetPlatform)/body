{
  this.platform=platform;
  this.project=project;
  this.lazyResolveCache=CachedValuesManager.getManager(project).createCachedValue(new CancelableResolveSessionValueProvider(project,platform),true);
}
