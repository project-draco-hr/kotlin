{
  JetType returnType=resolvedCall.getResultingDescriptor().getReturnType();
  CallInstruction instruction=CallInstruction.Factory.create(valueElement,getCurrentScope(),resolvedCall,receiverValues,arguments,returnType != null && KotlinBuiltIns.isNothing(returnType) ? null : valueFactory);
  add(instruction);
  return instruction;
}
