{
  MagicInstruction instruction=MagicInstruction.Factory.create(instructionElement,valueElement,getCurrentScope(),inputValues,expectedTypes,kind,valueFactory);
  add(instruction);
  return instruction;
}
