{
  if (scopeData.namedMembersMap == null) {
    if (psiClass != null) {
      scopeData.namedMembersMap=JavaDescriptorResolverHelper.getNamedMembers(new PsiClassWrapper(psiClass),staticMembers,scopeData.kotlin);
    }
 else {
      scopeData.namedMembersMap=Maps.newHashMap();
    }
  }
}
