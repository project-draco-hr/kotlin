{
  collectNamespacesAndClassifiers(outerScope,owner,declarations,context);
  createTypeConstructors(context);
  resolveTypesInClassHeaders(context);
  checkTypesInClassHeaders(context);
  return context;
}
