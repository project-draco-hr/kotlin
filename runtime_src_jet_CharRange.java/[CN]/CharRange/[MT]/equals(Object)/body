{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  CharRange range=(CharRange)o;
  return count == range.count && start == range.start;
}
