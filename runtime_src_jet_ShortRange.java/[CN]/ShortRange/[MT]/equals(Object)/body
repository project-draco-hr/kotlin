{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  ShortRange range=(ShortRange)o;
  return end == range.end && start == range.start;
}
