{
  JetExpression initializer=property.getInitializer();
  if (initializer == null) {
    return Collections.emptyList();
  }
  return generateInitializerForProperty(getPropertyDescriptor(context.bindingContext(),property),Translation.translateAsExpression(initializer,context),context);
}
