{
  for (  DeclarationDescriptor member : memberScope.getAllDescriptors()) {
    if (member instanceof DeclarationDescriptorWithVisibility && ((DeclarationDescriptorWithVisibility)member).getVisibility().isPublicAPI()) {
      member.acceptVoid(this);
    }
  }
  return visitDeclaration(descriptor);
}
