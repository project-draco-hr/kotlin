{
  List<ReferenceType> classes=positionManager.getAllClasses(position);
  assertNotNull(classes);
  assertEquals(1,classes.size());
  ReferenceType type=classes.get(0);
  if (!className.contains("$src$"))   assertEquals(className,type.name());
 else   assertTrue(type.name().startsWith(className));
}
