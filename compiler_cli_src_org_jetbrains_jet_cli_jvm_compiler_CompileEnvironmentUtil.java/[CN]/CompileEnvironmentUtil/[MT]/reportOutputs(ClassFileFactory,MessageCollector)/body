{
  for (  String outputFile : factory.files()) {
    List<File> sourceFiles=factory.getSourceFiles(outputFile);
    messageCollector.report(CompilerMessageSeverity.OUTPUT,OutputMessageUtil.formatOutputMessage(sourceFiles,new File(outputFile)),CompilerMessageLocation.NO_LOCATION);
  }
}
