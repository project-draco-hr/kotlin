{
  if (isNativeProperty(descriptor,context)) {
    return new NativePropertyAccessTranslator(descriptor,null,context);
  }
 else {
    return new KotlinPropertyAccessTranslator(descriptor,null,false,resolvedCall,context);
  }
}
