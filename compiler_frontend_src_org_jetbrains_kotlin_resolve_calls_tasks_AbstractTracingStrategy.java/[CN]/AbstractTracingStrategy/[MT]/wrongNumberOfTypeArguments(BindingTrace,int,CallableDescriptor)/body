{
  KtTypeArgumentList typeArgumentList=call.getTypeArgumentList();
  trace.report(WRONG_NUMBER_OF_TYPE_ARGUMENTS.on(typeArgumentList != null ? typeArgumentList : reference,expectedTypeArgumentCount,descriptor));
}
