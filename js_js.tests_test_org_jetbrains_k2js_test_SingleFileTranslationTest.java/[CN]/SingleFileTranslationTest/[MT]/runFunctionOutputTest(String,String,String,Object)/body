{
  EnumSet<EcmaVersion> ecmaVersions=EcmaVersion.all();
  generateJavaScriptFiles(kotlinFilename,MainCallParameters.noCall(),ecmaVersions);
  runRhinoTests(getOutputFilePaths(kotlinFilename,ecmaVersions),new RhinoFunctionResultChecker(namespaceName,functionName,expectedResult));
}
