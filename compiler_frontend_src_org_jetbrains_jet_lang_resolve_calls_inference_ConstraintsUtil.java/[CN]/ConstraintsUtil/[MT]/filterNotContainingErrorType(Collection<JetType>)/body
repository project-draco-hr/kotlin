{
  return Collections2.filter(types,new Predicate<JetType>(){
    @Override public boolean apply(    @Nullable JetType type){
      if (ErrorUtils.containsErrorType(type))       return false;
      return true;
    }
  }
);
}
