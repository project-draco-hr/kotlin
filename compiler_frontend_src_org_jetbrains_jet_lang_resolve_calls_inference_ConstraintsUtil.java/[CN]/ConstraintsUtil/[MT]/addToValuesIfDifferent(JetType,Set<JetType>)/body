{
  if (type == null)   return;
  if (values.isEmpty()) {
    values.add(type);
    return;
  }
  for (  JetType value : values) {
    if (!JetTypeChecker.INSTANCE.equalTypes(type,value)) {
      values.add(type);
      return;
    }
  }
}
