{
  collectNamespacesAndClassifiers(outerScope,owner,declarations);
  createTypeConstructors();
  resolveTypesInClassHeaders();
  checkGenericBoundsInClassHeaders();
  resolveFunctionAndPropertyHeaders(declarations);
  resolveBehaviorDeclarationBodies();
}
