{
  URLClassLoader classLoader=new URLClassLoader(new URL[]{bDir.toURI().toURL(),aDir.toURI().toURL(),ForTestCompileRuntime.runtimeJarForTests().toURI().toURL()},AbstractCompileKotlinAgainstKotlinTest.class.getClassLoader());
  Class<?> clazz=classLoader.loadClass(PackageClassUtils.getPackageClassName(FqName.ROOT));
  Method box=clazz.getMethod("box");
  String result=(String)box.invoke(null);
  assertEquals("OK",result);
}
