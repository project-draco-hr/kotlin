{
  List<TypeProjection> typeProjections=TypeUtils.getDefaultTypeProjections(getTypeConstructor().getParameters());
  return new JetTypeImpl(getAnnotations(),getTypeConstructor(),false,typeProjections,getMemberScope(typeProjections));
}
