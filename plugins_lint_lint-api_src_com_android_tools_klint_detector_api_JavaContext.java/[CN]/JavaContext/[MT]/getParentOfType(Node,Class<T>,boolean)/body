{
  if (element == null) {
    return null;
  }
  if (strict) {
    element=element.getParent();
  }
  while (element != null) {
    if (clz.isInstance(element)) {
      return (T)element;
    }
    element=element.getParent();
  }
  return null;
}
