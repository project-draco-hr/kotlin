{
  super.visitReferenceExpression(expression);
  final IdentifierImpl identifier=expression.getType() != null ? new IdentifierImpl(expression.getReferenceName()) : new IdentifierImpl(expression.getReferenceName(),false);
  myResult=new CallChain(expressionToExpression(expression.getQualifierExpression()),identifier);
}
