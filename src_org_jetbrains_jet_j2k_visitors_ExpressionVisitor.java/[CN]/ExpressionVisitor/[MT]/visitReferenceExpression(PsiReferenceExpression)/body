{
  super.visitReferenceExpression(expression);
  boolean isNullable=typeToType(expression.getType()).isNullable();
  myResult=new CallChainExpression(expressionToExpression(expression.getQualifierExpression()),new IdentifierImpl(expression.getReferenceName(),isNullable));
}
