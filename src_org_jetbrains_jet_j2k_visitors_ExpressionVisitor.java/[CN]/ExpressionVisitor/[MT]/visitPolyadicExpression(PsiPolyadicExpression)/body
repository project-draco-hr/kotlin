{
  super.visitPolyadicExpression(expression);
  if (expression.getOperationTokenType() != JavaTokenType.GTGTGT)   myResult=new PolyadicExpression(expressionsToExpressionList(expression.getOperands()),getOperatorString(expression.getOperationTokenType()));
 else {
  }
}
