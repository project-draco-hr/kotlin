{
  PsiElement context=expression.getContext();
  while (context != null) {
    if (context instanceof PsiMethod && ((PsiMethod)context).isConstructor())     return Converter.isConstructorPrimary((PsiMethod)context);
    context=context.getContext();
  }
  return false;
}
