{
  super.visitSuperExpression(expression);
  myResult=new SuperExpression(expression.getQualifier() != null ? new IdentifierImpl(expression.getQualifier().getQualifiedName()) : Identifier.EMPTY_IDENTIFIER);
}
