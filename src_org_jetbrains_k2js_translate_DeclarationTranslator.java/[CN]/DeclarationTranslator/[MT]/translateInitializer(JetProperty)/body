{
  JsExpression jsInitExpression=null;
  JetExpression initializer=declaration.getInitializer();
  if (initializer != null) {
    jsInitExpression=(JsExpression)(new ExpressionTranslator(translationContext())).translate(initializer);
  }
  return jsInitExpression;
}
