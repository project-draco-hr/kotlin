{
  if (initializer != null && initializer instanceof PsiReferenceExpression && ((PsiReferenceExpression)initializer).isQualified())   return createConversionForExpression(initializer,type);
  return "";
}
