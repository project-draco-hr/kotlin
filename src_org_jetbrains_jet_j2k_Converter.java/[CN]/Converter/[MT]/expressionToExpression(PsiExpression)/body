{
  if (e == null)   return new EmptyExpression();
  final ExpressionVisitor expressionVisitor=new ExpressionVisitor();
  e.accept(expressionVisitor);
  System.out.println(e.getClass());
  return expressionVisitor.getResult();
}
