{
  push(signatureVisitor().visitTypeArgument(toJetSignatureVariance(projectionKindForJava).getChar()));
  jetSignatureWriter.visitTypeArgument(toJetSignatureVariance(projectionKindForKotlin));
  generic=true;
}
