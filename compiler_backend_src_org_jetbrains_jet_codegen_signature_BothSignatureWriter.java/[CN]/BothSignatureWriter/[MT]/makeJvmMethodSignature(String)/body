{
  if (needGenerics) {
    return new JvmMethodSignature(makeAsmMethod(name),makeJavaString(),makeKotlinMethodTypeParameters(),makeKotlinParameterTypes(),makeKotlinReturnTypeSignature());
  }
 else {
    return new JvmMethodSignature(makeAsmMethod(name),makeKotlinParameterTypes());
  }
}
