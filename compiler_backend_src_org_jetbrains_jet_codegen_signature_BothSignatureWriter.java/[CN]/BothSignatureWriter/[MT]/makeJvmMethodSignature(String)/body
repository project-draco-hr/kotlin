{
  return new JvmMethodSignature(makeAsmMethod(name),needGenerics ? makeJavaGenericSignature() : null,needGenerics ? makeKotlinMethodTypeParameters() : null,makeKotlinParameterTypes(),makeKotlinReturnTypeSignature(),needGenerics);
}
