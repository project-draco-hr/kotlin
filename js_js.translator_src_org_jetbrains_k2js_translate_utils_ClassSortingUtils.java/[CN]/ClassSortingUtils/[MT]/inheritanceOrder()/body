{
  return new PartiallyOrderedSet.Order<ClassDescriptor>(){
    @Override public boolean firstDependsOnSecond(    @NotNull ClassDescriptor first,    @NotNull ClassDescriptor second){
      return isDerivedClass(first,second);
    }
  }
;
}
