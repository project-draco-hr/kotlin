{
  try {
    return doExec(messageCollector,environment,out,module);
  }
 catch (  Throwable e) {
    messageCollector.report(CompilerMessageSeverity.ERROR,"Exception while executing compiler:\n" + e.getMessage(),CompilerMessageLocation.NO_LOCATION);
  }
  return -1;
}
