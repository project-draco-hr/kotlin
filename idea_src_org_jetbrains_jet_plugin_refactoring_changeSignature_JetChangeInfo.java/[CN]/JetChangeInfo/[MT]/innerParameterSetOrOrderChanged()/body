{
  if (newParameters.size() != oldDescriptor.getParametersCount())   return true;
  for (int i=0; i < newParameters.size(); i++) {
    if (newParameters.get(i).getOldIndex() != i)     return true;
  }
  return false;
}
