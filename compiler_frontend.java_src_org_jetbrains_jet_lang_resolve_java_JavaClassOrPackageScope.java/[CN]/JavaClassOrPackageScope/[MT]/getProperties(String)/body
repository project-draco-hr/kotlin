{
  PsiClassWrapper psiClass=memoPsiClass();
  if (psiClass == null) {
    return Collections.emptySet();
  }
  return semanticServices.getDescriptorResolver().resolveFieldGroupByName(descriptor,psiClass.getPsiClass(),name,staticMembers());
}
