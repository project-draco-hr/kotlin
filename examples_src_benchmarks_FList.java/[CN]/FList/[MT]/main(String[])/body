{
  for (int k=0; k != 10; ++k) {
    long start=System.currentTimeMillis();
    FList<Integer> list=new Empty();
    for (int i=0; i != 5000000; ++i)     list=list.plus(i);
    System.out.println(System.currentTimeMillis() - start);
  }
}
