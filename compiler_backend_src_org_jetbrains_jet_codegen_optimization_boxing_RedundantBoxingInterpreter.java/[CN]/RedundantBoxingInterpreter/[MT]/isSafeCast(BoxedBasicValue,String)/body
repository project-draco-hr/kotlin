{
  try {
    return Class.forName(Type.getObjectType(descTo).getClassName()).isAssignableFrom(Class.forName(value.getType().getClassName()));
  }
 catch (  ClassNotFoundException e) {
    return false;
  }
}
