{
  JsExpression jsInitExpression=null;
  JetExpression initializer=declaration.getInitializer();
  if (initializer != null) {
    jsInitExpression=AstUtil.convertToExpression(Translation.expressionTranslator(context).translate(initializer));
  }
  return jsInitExpression;
}
