{
  LanguageFeatureSettings result=null;
  for (  TestFile file : module) {
    if (file.customLanguageFeatureSettings != null) {
      if (result != null) {
        Assert.fail("More than one file in the module has " + BaseDiagnosticsTest.LANGUAGE_DIRECTIVE + " directive specified. "+ "This is not supported. Please move all directives into one file");
      }
      result=file.customLanguageFeatureSettings;
    }
  }
  return result;
}
