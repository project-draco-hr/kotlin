{
  final PsiAnnotation annotation=JavaDescriptorResolver.findAnnotation(psiMethod,JvmStdlibNames.KOTLIN_SIGNATURE.getFqName().getFqName());
  return annotation != null ? new KotlinSignatureAnnotation(annotation) : NULL_ANNOTATION;
}
