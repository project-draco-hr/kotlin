{
  try {
    String line=consoleReader.readLine(">>> ");
    if (line == null) {
      return false;
    }
    if (line.startsWith(":")) {
      return oneCommand(line.substring(1));
    }
    eval(line);
    return true;
  }
 catch (  Exception e) {
    throw ExceptionUtils.rethrow(e);
  }
}
