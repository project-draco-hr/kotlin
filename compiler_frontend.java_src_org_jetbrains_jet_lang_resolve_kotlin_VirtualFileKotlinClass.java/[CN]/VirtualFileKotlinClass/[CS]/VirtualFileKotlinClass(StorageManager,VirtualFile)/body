{
  this.file=file;
  this.className=storageManager.createLazyValue(new Function0<JvmClassName>(){
    @Override public JvmClassName invoke(){
      return computeClassName();
    }
  }
);
  this.classHeader=storageManager.createNullableLazyValue(new Function0<KotlinClassHeader>(){
    @Override public KotlinClassHeader invoke(){
      return ReadKotlinClassHeaderAnnotationVisitor.read(VirtualFileKotlinClass.this);
    }
  }
);
}
