{
  JvmProtoBuf.JvmFieldSignature.Builder builder=JvmProtoBuf.JvmFieldSignature.newBuilder().setName(stringTable.getStringIndex(name)).setDesc(stringTable.getStringIndex(desc));
  if (isStaticInOuter) {
    builder.setIsStaticInOuter(true);
  }
  return builder.build();
}
