{
  JetExpression loopRange=forExpression.getLoopRange();
  ResolvedCall<FunctionDescriptor> nextCall=getNotNull(bindingContext,LOOP_RANGE_NEXT_RESOLVED_CALL,loopRange,"No next() function " + DiagnosticUtils.atLocation(loopRange));
  return nextCall.getResultingDescriptor().getReturnType();
}
