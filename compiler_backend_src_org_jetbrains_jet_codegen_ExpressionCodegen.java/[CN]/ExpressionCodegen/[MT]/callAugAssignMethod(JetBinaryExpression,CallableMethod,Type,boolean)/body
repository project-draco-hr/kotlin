{
  StackValue value=gen(expression.getLeft());
  if (keepReturnValue) {
    value.dupReceiver(v);
  }
  value.put(lhsType,v);
  genToJVMStack(expression.getRight());
  callable.invoke(v);
  if (keepReturnValue) {
    value.store(v);
  }
}
