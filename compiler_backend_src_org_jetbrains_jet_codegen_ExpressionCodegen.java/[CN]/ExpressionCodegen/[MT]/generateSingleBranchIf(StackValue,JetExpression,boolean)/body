{
  Label end=continueLabel != null ? continueLabel : new Label();
  condition.condJump(end,inverse,v);
  gen(expression,Type.VOID_TYPE);
  if (continueLabel != end)   v.mark(end);
  return StackValue.none();
}
