{
  return (descriptor instanceof NamespaceDescriptor) || (descriptor instanceof ClassifierDescriptor) || ((descriptor instanceof PropertyDescriptor) && (trace.get(BindingContext.OBJECT_DECLARATION_CLASS,((PropertyDescriptor)descriptor)) != null));
}
