{
  if (context.getPhase() == 1) {
    mNames=new HashSet<String>();
  }
  mIgnoreFile=context.file.getName().startsWith("donottranslate") || UnusedResourceDetector.isAnalyticsFile(context);
  if (!context.getProject().getReportIssues()) {
    mIgnoreFile=true;
  }
}
