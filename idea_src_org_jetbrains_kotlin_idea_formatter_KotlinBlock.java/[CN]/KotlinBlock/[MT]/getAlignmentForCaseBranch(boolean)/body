{
  if (shouldAlignInColumns) {
    return NodeAlignmentStrategy.fromTypes(AlignmentStrategy.createAlignmentPerTypeStrategy(Arrays.asList((IElementType)ARROW),WHEN_ENTRY,true));
  }
 else {
    return NodeAlignmentStrategy.getNullStrategy();
  }
}
