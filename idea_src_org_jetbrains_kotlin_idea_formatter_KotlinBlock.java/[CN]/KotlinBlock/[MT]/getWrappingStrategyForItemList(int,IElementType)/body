{
  final Wrap itemWrap=Wrap.createWrap(wrapType,false);
  return new WrappingStrategy(){
    @Nullable @Override public Wrap getWrap(    @NotNull IElementType childElementType){
      return childElementType == itemType ? itemWrap : null;
    }
  }
;
}
