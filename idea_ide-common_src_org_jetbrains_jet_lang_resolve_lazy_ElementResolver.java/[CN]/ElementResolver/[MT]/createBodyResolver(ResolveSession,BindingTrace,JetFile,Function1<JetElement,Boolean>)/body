{
  InjectorForBodyResolve bodyResolve=new InjectorForBodyResolve(file.getProject(),createParameters(resolveSession,statementFilter),trace,resolveSession.getModuleDescriptor(),getAdditionalCheckerProvider(file));
  return bodyResolve.getBodyResolver();
}
