{
  jetLTTyped='<' == c && file instanceof JetFile && CodeInsightSettings.getInstance().AUTOINSERT_PAIR_BRACKET && JetLtGtTypingUtils.shouldAutoCloseAngleBracket(editor.getCaretModel().getOffset(),editor);
  if ('>' == c) {
    if (file instanceof JetFile && CodeInsightSettings.getInstance().AUTOINSERT_PAIR_BRACKET) {
      if (JetLtGtTypingUtils.handleJetGTInsert(editor)) {
        return Result.STOP;
      }
    }
  }
  return super.beforeCharTyped(c,project,editor,file,fileType);
}
