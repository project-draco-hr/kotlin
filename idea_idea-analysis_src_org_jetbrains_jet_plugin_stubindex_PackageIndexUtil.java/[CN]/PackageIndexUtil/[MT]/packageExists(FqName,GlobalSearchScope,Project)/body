{
  final Ref<Boolean> result=new Ref<Boolean>(false);
  StubIndex.getInstance().processElements(JetAllPackagesIndex.getInstance().getKey(),packageFqName.asString(),project,searchScope,JetFile.class,new Processor<JetFile>(){
    @Override public boolean process(    JetFile file){
      result.set(true);
      return false;
    }
  }
);
  return result.get();
}
