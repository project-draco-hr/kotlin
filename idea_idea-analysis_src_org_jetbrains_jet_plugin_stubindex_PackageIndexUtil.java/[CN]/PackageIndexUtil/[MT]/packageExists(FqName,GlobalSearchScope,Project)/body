{
  return containsAny(packageFqName,searchScope,project,JetExactPackagesIndex.getInstance().getKey()) || SubpackagesIndexService.OBJECT$.getInstance(project).hasSubpackages(packageFqName,searchScope);
}
