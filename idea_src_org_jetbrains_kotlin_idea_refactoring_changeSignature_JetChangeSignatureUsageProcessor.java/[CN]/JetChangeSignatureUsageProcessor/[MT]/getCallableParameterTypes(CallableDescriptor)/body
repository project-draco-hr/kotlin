{
  return ContainerUtil.map(descriptor.getValueParameters(),new Function<ValueParameterDescriptor,KtType>(){
    @Override public KtType fun(    ValueParameterDescriptor descriptor){
      return descriptor.getType();
    }
  }
);
}
