{
  DeclarationDescriptor descriptor=getDescriptorForReferenceExpression(context.bindingContext(),expression.getInstanceReference());
  if (context.aliaser().hasAliasForThis(descriptor)) {
    return context.aliaser().getAliasForThis(descriptor);
  }
  return new JsThisRef();
}
