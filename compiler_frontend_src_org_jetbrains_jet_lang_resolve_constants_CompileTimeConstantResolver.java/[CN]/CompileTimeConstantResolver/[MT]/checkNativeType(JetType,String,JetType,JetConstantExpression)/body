{
  if (!noExpectedTypeOrError(expectedType) && !JetTypeChecker.INSTANCE.isSubtypeOf(nativeType,expectedType)) {
    return createErrorValue(CONSTANT_EXPECTED_TYPE_MISMATCH.on(expression,title,expectedType));
  }
  return null;
}
