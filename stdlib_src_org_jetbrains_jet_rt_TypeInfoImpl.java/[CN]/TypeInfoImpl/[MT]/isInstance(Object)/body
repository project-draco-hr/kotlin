{
  if (obj == null)   return nullable;
  if (obj instanceof JetObject) {
    return ((TypeInfoImpl)((JetObject)obj).getTypeInfo()).isSubtypeOf(this);
  }
  return signature.klazz.isAssignableFrom(obj.getClass());
}
