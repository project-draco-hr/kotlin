{
  CommandProcessor.getInstance().executeCommand(myFixture.getProject(),new KotlinImportOptimizer().processFile(myFixture.getFile()),"Optimize Imports",null,UndoConfirmationPolicy.DO_NOT_REQUEST_CONFIRMATION);
}
