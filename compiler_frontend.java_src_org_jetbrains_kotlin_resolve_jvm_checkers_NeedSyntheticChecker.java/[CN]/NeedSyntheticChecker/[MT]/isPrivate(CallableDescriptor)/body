{
  return Visibilities.isPrivate(targetDescriptor.getVisibility()) || (targetDescriptor instanceof PropertyDescriptor && ((PropertyDescriptor)targetDescriptor).getSetter() != null && Visibilities.isPrivate(((PropertyDescriptor)targetDescriptor).getSetter().getVisibility()));
}
