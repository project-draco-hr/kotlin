{
  registerEarlierScripts(earlierScripts);
  registerClassNameForScript(state.getBindingTrace(),script,className);
  state.beforeCompile();
  GenerationStrategy.STANDARD.generateNamespace(state,JetPsiUtil.getFQName((JetFile)script.getContainingFile()),Collections.singleton((JetFile)script.getContainingFile()),errorHandler);
}
