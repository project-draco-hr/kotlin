{
  DeclarationDescriptor propertyContainer=propertyDescriptor.getContainingDeclaration();
  return propertyDescriptor.isConst() && isInterfaceCompanionObject(propertyContainer) && propertyDescriptor.getVisibility() == Visibilities.PUBLIC;
}
