{
  List<String> args=new ArrayList<String>();
  if (metaFile != null) {
    args.add("-meta-info");
  }
  if (kjsm) {
    args.add("-kjsm");
  }
  args.add("-output");
  args.add(outputFile.getAbsolutePath());
  args.add(sourcesPath);
  runJsCompiler(args);
}
