{
  return ContainerUtil.getOrCreate(samInterfaceToWrapperClass,Pair.create(samInterface,file),new Factory<JvmClassName>(){
    @Override public JvmClassName create(){
      return new SamWrapperCodegen(state,samInterface).genWrapper(file);
    }
  }
);
}
