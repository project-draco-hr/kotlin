{
  return ContainerUtil.getOrCreate(samInterfaceToWrapperClass,Pair.create(samInterface,file),new Factory<Type>(){
    @Override public Type create(){
      return new SamWrapperCodegen(state,samInterface).genWrapper(file);
    }
  }
);
}
