{
  final StringBuilder builder=new StringBuilder();
  tuple.forEach(new Function1<Object,Void>(){
    @Override public Void invoke(    Object o){
      builder.append(o);
      return null;
    }
  }
);
  return builder.toString();
}
