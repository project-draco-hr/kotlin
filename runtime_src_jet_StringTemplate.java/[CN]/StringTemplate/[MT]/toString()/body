{
  final StringBuilder builder=new StringBuilder();
  tuple.forEach(new Function1<Object,Tuple0>(){
    @Override public Tuple0 invoke(    Object o){
      builder.append(o);
      return Tuple0.VALUE;
    }
  }
);
  return builder.toString();
}
