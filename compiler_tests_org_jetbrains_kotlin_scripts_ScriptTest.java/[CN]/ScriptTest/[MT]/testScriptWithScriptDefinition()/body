{
  Class<?> aClass=compileScript("fib.fib.kt",null,Collections.singletonList(new KotlinScriptDefinition(".fib.kt",numIntParam())));
  Assert.assertNotNull(aClass);
  aClass.getConstructor(int.class).newInstance(4);
}
