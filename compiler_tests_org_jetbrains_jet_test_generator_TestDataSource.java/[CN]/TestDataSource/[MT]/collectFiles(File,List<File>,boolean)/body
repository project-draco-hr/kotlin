{
  for (  File file : current.listFiles(filter)) {
    if (file.isDirectory()) {
      if (recursive) {
        collectFiles(file,result,recursive);
      }
    }
 else {
      result.add(file);
    }
  }
}
