{
  try {
    run(args);
  }
 catch (  Throwable e) {
    System.err.println("error: " + e.toString());
    e.printStackTrace();
    System.err.println();
    printUsage(System.err);
    System.exit(1);
  }
}
