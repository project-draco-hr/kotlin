{
  if (!ComparatorUtil.equalsNullable(fieldWrapper.getName(),namedElement.getName())) {
    throw new AlternativeSignatureMismatchException("Field name mismatch, original: %s, alternative: %s",fieldWrapper.getName(),namedElement.getName());
  }
}
