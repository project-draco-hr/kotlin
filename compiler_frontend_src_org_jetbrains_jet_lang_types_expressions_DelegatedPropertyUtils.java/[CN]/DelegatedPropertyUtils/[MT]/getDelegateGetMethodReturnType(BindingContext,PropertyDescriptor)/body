{
  ResolvedCall<FunctionDescriptor> resolvedCall=context.get(DELEGATED_PROPERTY_RESOLVED_CALL,descriptor.getGetter());
  return resolvedCall != null ? resolvedCall.getResultingDescriptor().getReturnType() : null;
}
