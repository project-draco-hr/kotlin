{
  myBindingContext=AnalyzerFacade.analyzeFilesWithJavaIntegration(myEnvironment.getProject(),mySourceFiles,Predicates.<PsiFile>alwaysTrue(),JetControlFlowDataTraceFactory.EMPTY);
  for (  Diagnostic diagnostic : myBindingContext.getDiagnostics()) {
    errorCollector.report(diagnostic);
  }
}
