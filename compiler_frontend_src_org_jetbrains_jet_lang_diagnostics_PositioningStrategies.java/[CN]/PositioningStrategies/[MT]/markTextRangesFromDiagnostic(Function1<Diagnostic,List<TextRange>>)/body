{
  return new PositioningStrategy<PsiElement>(){
    @NotNull @Override public List<TextRange> markDiagnostic(    @NotNull ParametrizedDiagnostic<? extends PsiElement> diagnostic){
      return getTextRanges.invoke(diagnostic);
    }
  }
;
}
