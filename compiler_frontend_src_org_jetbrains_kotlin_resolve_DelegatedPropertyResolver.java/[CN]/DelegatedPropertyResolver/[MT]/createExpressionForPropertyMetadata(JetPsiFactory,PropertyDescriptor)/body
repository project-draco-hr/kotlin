{
  return psiFactory.createExpression(builtIns.getPropertyMetadataImpl().getName().asString() + "(\"" + propertyDescriptor.getName().asString()+ "\"): "+ builtIns.getPropertyMetadata().getName().asString());
}
