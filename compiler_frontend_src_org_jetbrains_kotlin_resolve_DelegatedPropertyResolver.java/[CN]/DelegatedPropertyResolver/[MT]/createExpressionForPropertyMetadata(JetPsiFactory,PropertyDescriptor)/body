{
  return psiFactory.createExpression(builtIns.getPropertyMetadataImpl().getName().asString() + "(\"" + propertyDescriptor.getName().asString()+ "\") as "+ builtIns.getPropertyMetadata().getName().asString());
}
