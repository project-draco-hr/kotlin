{
  URLClassLoader classLoader=new URLClassLoader(new URL[]{bDir.toURI().toURL(),aDir.toURI().toURL()},ForTestCompileRuntime.runtimeJarClassLoader());
  return classLoader.loadClass(PackageClassUtils.getPackageClassName(FqName.ROOT));
}
