{
  if (!exceptOwn) {
    if (!CodegenUtil.isInterface(type))     if (isParametrizedClass(type))     return true;
  }
  for (  JetType jetType : type.getConstructor().getSupertypes()) {
    if (hasDerivedTypeInfoField(jetType,false))     return true;
  }
  return false;
}
