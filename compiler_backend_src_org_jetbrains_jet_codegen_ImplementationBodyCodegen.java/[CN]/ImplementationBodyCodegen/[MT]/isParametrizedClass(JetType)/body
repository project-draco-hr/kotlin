{
  if (type.getConstructor().getParameters().size() > 0)   return true;
  for (  JetType jetType : type.getConstructor().getSupertypes()) {
    if (isParametrizedClass(jetType))     return true;
  }
  return false;
}
