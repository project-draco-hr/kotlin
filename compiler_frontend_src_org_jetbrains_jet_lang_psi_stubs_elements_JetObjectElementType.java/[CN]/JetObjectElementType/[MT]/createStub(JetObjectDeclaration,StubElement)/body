{
  String name=psi.getName();
  FqName fqName=ResolveSessionUtils.safeFqNameForLazyResolve(psi);
  List<String> superNames=PsiUtilPackage.getSuperNames(psi);
  return new PsiJetObjectStubImpl(parentStub,StringRef.fromString(name),fqName,Utils.instance$.wrapStrings(superNames),psi.isTopLevel(),isClassObject(psi),JetPsiUtil.isLocal(psi),psi.isObjectLiteral());
}
