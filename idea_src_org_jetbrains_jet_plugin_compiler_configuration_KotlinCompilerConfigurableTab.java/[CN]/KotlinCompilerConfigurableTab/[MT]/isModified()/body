{
  return ComparingUtils.isModified(generateNoWarningsCheckBox,isGenerateNoWarnings()) || ComparingUtils.isModified(additionalArgsOptionsField,additionalCompilerSettings.getAdditionalArguments());
}
