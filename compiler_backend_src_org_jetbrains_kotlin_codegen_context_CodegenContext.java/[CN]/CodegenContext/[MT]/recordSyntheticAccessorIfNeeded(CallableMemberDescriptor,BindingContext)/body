{
  if (hasThisDescriptor() && (descriptor instanceof ConstructorDescriptor || Boolean.TRUE.equals(bindingContext.get(NEED_SYNTHETIC_ACCESSOR,descriptor)))) {
    accessibleDescriptorIfNeeded(descriptor);
  }
}
