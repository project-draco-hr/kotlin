{
  if (type == Type.VOID_TYPE) {
    myOperand.put(type,v);
    return;
  }
  if (type != Type.BOOLEAN_TYPE) {
    throw new UnsupportedOperationException("don't know how to put a compare as a non-boolean type");
  }
  putAsBoolean(v);
}
