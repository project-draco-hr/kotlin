{
  if (stackValue instanceof StackValueWithReceiver) {
    return new Delegated(stackValue.type,(StackValueWithReceiver)stackValue,new ComplexReceiver((StackValueWithSimpleReceiver)stackValue,operations));
  }
 else {
    return stackValue;
  }
}
