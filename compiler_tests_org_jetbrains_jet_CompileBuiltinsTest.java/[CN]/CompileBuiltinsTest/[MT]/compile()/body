{
  KotlinCompiler.ExitCode exitCode=new KotlinCompiler().exec(System.err,"-output",tmpdir.getPath(),"-src","./compiler/frontend/src","-stubs");
  if (exitCode != KotlinCompiler.ExitCode.OK) {
    throw new IllegalStateException("jdk headers compilation failed: " + exitCode);
  }
}
