{
  JetChangeSignatureDialog dialog=new JetChangeSignatureDialog(project,platformDescriptor,context,getText());
  if (ApplicationManager.getApplication().isUnitTestMode())   performRefactoringSilently(dialog);
 else   dialog.show();
}
