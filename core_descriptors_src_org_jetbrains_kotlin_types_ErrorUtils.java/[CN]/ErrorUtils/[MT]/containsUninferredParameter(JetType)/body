{
  return TypeUtils.containsSpecialType(type,new Function1<JetType,Boolean>(){
    @Override public Boolean invoke(    JetType argumentType){
      return isUninferredParameter(argumentType);
    }
  }
);
}
