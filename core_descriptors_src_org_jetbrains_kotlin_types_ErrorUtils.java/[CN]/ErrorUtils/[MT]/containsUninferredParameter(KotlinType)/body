{
  return TypeUtils.contains(type,new Function1<KotlinType,Boolean>(){
    @Override public Boolean invoke(    KotlinType argumentType){
      return isUninferredParameter(argumentType);
    }
  }
);
}
