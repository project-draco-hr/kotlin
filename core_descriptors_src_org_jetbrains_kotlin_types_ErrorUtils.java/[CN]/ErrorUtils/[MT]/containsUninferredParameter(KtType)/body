{
  return TypeUtils.containsSpecialType(type,new Function1<KtType,Boolean>(){
    @Override public Boolean invoke(    KtType argumentType){
      return isUninferredParameter(argumentType);
    }
  }
);
}
