{
  if (functionDescriptor.getTypeParameters().isEmpty()) {
    return functionDescriptor;
  }
  return new FunctionDescriptorImpl(functionDescriptor,functionDescriptor.getAttributes(),functionDescriptor.getName(),Collections.<TypeParameterDescriptor>emptyList(),getSubstitutedValueParameters(functionDescriptor,typeArguments),getSubstitutedReturnType(functionDescriptor,typeArguments));
}
