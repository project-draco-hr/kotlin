{
  ApplicationManager.getApplication().assertReadAccessAllowed();
synchronized (declarationAnalysisLock) {
    return CachedValuesManager.getManager(project).getCachedValue(project,KOTLIN_JS_DECLARATIONS_CACHE,new KotlinDeclarationsJsCacheProvider(),false);
  }
}
