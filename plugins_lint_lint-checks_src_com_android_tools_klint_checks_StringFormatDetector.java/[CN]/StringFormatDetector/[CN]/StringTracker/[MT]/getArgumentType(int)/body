{
  Expression arg=getArgument(argument);
  if (arg != null) {
    Class<?> type=getType(arg);
    if (type != null) {
      return type;
    }
    if (mContext != null) {
      return getTypeClass(mContext.getType(arg));
    }
  }
  return null;
}
