{
  Label ifTrue=new Label();
  Label ifFalse=new Label();
  Label end=new Label();
  condJump(ifTrue,ifFalse,v);
  v.mark(ifFalse);
  v.iconst(0);
  v.goTo(end);
  v.mark(ifTrue);
  v.iconst(1);
  v.mark(end);
}
