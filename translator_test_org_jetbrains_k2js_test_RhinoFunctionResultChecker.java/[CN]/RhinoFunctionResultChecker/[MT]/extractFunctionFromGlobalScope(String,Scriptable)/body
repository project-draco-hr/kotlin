{
  Object functionObject;
  functionObject=scope.get(functionName,scope);
  assertTrue("Function " + functionName + " is not defined in global scope",functionObject instanceof Function);
  return functionObject;
}
