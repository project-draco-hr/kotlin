{
  return KotlinPackage.flatMap(getAnnotations(),new Function1<JetAnnotation,List<JetAnnotationEntry>>(){
    @Override public List<JetAnnotationEntry> invoke(    JetAnnotation annotation){
      return annotation.getEntries();
    }
  }
);
}
