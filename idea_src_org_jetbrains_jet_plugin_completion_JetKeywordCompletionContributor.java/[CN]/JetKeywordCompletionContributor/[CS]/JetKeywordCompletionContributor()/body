{
  PsiElementPattern.Capture<PsiElement> notDotPlace=PlatformPatterns.psiElement().and(new FilterPattern(new NotFilter(new LeftNeighbour(new TextFilter(".")))));
  extend(CompletionType.BASIC,notDotPlace,new JetTopKeywordCompletionProvider());
}
