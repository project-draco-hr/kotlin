{
  List<File> r=Lists.newArrayList();
  File[] files=dir.listFiles();
  if (files != null) {
    for (    File jar : files) {
      if (jar.isFile() && jar.getName().endsWith(".jar")) {
        r.add(jar);
      }
    }
  }
  return r;
}
