{
  return CollectionsKt.map(getContainingDeclaration().getOverriddenDescriptors(),new Function1<CallableDescriptor,ValueParameterDescriptor>(){
    @Override public ValueParameterDescriptor invoke(    CallableDescriptor descriptor){
      return descriptor.getValueParameters().get(getIndex());
    }
  }
);
}
