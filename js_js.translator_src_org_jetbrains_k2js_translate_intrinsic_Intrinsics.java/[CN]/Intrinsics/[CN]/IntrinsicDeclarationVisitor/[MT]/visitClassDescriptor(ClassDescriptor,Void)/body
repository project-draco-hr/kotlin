{
  for (  DeclarationDescriptor memberDescriptor : descriptor.getDefaultType().getMemberScope().getAllDescriptors(DescriptorPredicate.all())) {
    memberDescriptor.accept(this,null);
  }
  return null;
}
