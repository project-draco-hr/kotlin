{
  return createBodyResolver(trace,file,new BodyResolveContextForLazy(resolveSession.getStorageManager(),resolveSession.getExceptionTracker(),Functions.<JetScope>constant(null)),resolveSession.getModuleDescriptor());
}
