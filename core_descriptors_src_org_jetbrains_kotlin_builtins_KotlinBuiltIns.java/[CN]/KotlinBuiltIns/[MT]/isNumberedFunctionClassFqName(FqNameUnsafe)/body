{
  List<Name> segments=fqName.pathSegments();
  if (segments.size() != 2)   return false;
  if (!BUILT_INS_PACKAGE_NAME.equals(first(segments)))   return false;
  return BuiltInFictitiousFunctionClassFactory.parseClassName(last(segments).asString(),FunctionClassDescriptor.Kinds.Functions) != null;
}
