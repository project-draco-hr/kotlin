{
  try {
    K2JSTranslator.translateWithMainCallParametersAndSaveToFile(mainCall,environmentForJS.getSourceFiles(),outputFile,outputPrefix,outputPostfix,config);
  }
 catch (  Exception e) {
    throw new RuntimeException(e);
  }
  return OK;
}
