{
  if (aliasingMap != null) {
    JsNameRef name=aliasingMap.get(BindingUtils.getClassForDescriptor(bindingContext(),superClassDescriptor),(JetClass)classDeclaration);
    if (name != null) {
      return name;
    }
  }
  return getQualifiedReference(context(),superClassDescriptor);
}
