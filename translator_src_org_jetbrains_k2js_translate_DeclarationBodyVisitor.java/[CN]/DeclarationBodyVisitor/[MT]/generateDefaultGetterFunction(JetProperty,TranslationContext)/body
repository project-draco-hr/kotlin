{
  JsReturn returnExpression=new JsReturn(backingFieldReference(expression,context));
  return AstUtil.newFunction(context.enclosingScope(),null,new ArrayList<JsParameter>(),AstUtil.convertToBlock(returnExpression));
}
