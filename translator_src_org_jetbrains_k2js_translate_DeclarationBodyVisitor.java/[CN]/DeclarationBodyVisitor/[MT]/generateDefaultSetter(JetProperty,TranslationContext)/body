{
  PropertySetterDescriptor setterDescriptor=BindingUtils.getPropertySetterDescriptorForProperty(context.bindingContext(),expression);
  return AstUtil.newNamedMethod(context.getNameForDescriptor(setterDescriptor),generateDefaultSetterFunction(expression,context.newPropertyAccess(setterDescriptor)));
}
