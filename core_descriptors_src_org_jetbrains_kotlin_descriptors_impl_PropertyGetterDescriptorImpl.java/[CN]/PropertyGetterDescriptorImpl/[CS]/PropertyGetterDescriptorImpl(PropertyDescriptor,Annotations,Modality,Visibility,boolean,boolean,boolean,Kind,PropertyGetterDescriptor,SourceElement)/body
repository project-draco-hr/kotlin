{
  super(modality,visibility,correspondingProperty,annotations,Name.special("<get-" + correspondingProperty.getName() + ">"),hasBody,isDefault,isExternal,kind,source);
  this.original=original != null ? original : this;
}
