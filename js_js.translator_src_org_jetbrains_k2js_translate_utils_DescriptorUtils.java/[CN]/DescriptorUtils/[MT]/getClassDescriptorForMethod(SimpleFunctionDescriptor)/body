{
  if (!functionDescriptor.getExpectedThisObject().exists()) {
    return null;
  }
  ClassifierDescriptor descriptor=functionDescriptor.getExpectedThisObject().getType().getConstructor().getDeclarationDescriptor();
  if (descriptor instanceof ClassDescriptor) {
    return (ClassDescriptor)descriptor.getOriginal();
  }
  return null;
}
