{
  for (  PsiReference r : expression.getReferences())   if (r.getCanonicalText().equals("this"))   if (r.resolve() != null && r.resolve() instanceof PsiMethod && ((PsiMethod)r.resolve()).isConstructor())   myResolvedConstructors.add((PsiMethod)r.resolve());
}
