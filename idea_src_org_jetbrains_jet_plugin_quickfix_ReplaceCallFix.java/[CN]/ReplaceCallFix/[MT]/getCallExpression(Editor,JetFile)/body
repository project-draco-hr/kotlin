{
  final PsiElement elementAtCaret=getElementAtCaret(editor,file);
  return PsiTreeUtil.getParentOfType(elementAtCaret,fromDot || !safe ? JetDotQualifiedExpression.class : JetSafeQualifiedExpression.class);
}
