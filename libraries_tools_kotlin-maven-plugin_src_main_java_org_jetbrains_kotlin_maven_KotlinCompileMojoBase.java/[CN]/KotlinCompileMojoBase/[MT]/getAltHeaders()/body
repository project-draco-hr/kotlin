{
  if (altHeadersPath != null)   return altHeadersPath;
  try {
    altHeadersPath=extractAltHeaders();
    if (altHeadersPath == null)     throw new RuntimeException("Can't find kotlin alt headers in maven plugin resources");
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
  return altHeadersPath;
}
