{
  Set<Descriptor> candidates=Sets.newLinkedHashSet();
  outerLoop:   for (  Descriptor me : candidateSet) {
    for (    Descriptor other : candidateSet) {
      if (OverloadingConflictResolver.overrides(other,me)) {
        continue outerLoop;
      }
    }
    candidates.add(me);
  }
  return candidates;
}
