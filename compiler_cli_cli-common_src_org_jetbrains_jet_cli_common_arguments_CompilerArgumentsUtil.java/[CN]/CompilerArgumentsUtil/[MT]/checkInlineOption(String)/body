{
  if (option == null || "on".equalsIgnoreCase(option) || "off".equalsIgnoreCase(option) || "true".equalsIgnoreCase(option) || "false".equalsIgnoreCase(option)) {
    return true;
  }
  return false;
}
