{
  if (appropriateQuickFix != null) {
    return appropriateQuickFix.getText();
  }
  return KotlinBundle.message("change.function.literal.return.type",IdeDescriptorRenderers.SOURCE_CODE_SHORT_NAMES_IN_TYPES.renderType(type));
}
