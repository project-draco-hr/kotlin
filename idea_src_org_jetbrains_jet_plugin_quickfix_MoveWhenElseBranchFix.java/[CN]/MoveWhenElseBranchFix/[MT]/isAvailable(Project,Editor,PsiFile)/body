{
  if (!super.isAvailable(project,editor,file)) {
    return false;
  }
  int elseCount=0;
  for (  JetWhenEntry entry : element.getEntries()) {
    if (entry.isElse()) {
      elseCount++;
    }
  }
  return (elseCount == 1);
}
