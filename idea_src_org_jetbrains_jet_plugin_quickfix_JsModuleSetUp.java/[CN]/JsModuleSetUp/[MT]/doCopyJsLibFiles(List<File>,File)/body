{
  try {
    File lib=new File(rootDir,"lib");
    for (    File file : files) {
      copyFileToDir(file,lib);
    }
  }
 catch (  IOException e) {
    notifyFailure("Failed to copy file: " + e.getMessage());
    return false;
  }
  return true;
}
