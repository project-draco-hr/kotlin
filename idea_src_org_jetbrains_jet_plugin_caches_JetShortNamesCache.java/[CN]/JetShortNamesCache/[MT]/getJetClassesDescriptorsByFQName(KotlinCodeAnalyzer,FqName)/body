{
  Collection<JetClassOrObject> jetClassOrObjects=JetFullClassNameIndex.getInstance().get(classFQName.getFqName(),project,GlobalSearchScope.allScope(project));
  if (jetClassOrObjects.isEmpty()) {
    return Collections.emptyList();
  }
  return ResolveSessionUtils.getClassDescriptorsByFqName(analyzer,classFQName);
}
