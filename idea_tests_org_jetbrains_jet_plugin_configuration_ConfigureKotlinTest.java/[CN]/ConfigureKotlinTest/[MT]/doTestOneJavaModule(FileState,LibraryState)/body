{
  doTestOneModule(jarState,libraryState,JAVA_CONFIGURATOR);
  assertFalse("Module " + getModule().getName() + "  should not be configured as JavaScript Module",JS_CONFIGURATOR.isConfigured(getModule()));
}
