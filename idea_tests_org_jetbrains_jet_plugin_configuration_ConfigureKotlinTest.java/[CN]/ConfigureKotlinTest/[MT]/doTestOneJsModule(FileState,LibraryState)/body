{
  doTestOneModule(jarState,libraryState,JS_CONFIGURATOR);
  assertFalse("Module " + getModule().getName() + " should not be configured as Java Module",JAVA_CONFIGURATOR.isConfigured(getModule()));
}
