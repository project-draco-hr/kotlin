{
  return KotlinPackage.map(qualifiedNames.getAllInternedObjects(),new Function1<FqNameProto,QualifiedName.Builder>(){
    @Override public QualifiedName.Builder invoke(    FqNameProto proto){
      return proto.fqName;
    }
  }
);
}
