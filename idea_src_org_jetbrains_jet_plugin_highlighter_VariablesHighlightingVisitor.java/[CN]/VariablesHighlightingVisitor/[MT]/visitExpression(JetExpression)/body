{
  JetType autoCast=bindingContext.get(AUTOCAST,expression);
  if (autoCast != null) {
    holder.createInfoAnnotation(expression,"Automatically cast to " + DescriptorRendererImpl.TEXT.renderType(autoCast)).setTextAttributes(JetHighlightingColors.AUTO_CASTED_VALUE);
  }
  super.visitExpression(expression);
}
