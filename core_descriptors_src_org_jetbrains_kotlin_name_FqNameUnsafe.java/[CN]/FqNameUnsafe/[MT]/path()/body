{
  final List<FqNameUnsafe> path=new ArrayList<FqNameUnsafe>();
  path.add(FqName.ROOT.toUnsafe());
  walk(new WalkCallback(){
    @Override public void segment(    @NotNull Name shortName,    @NotNull FqNameUnsafe fqName){
      path.add(fqName);
    }
  }
);
  return path;
}
