{
  return isRoot() ? Collections.<Name>emptyList() : ArraysKt.map(fqName.split("\\."),new Function1<String,Name>(){
    @Override public Name invoke(    String name){
      return Name.guessByFirstCharacter(name);
    }
  }
);
}
