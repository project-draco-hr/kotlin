{
  if (type == TypeUtils.NO_EXPECTED_TYPE || type == DONT_CARE || type == CANT_INFER) {
    return true;
  }
  if (type == null || ((ErrorUtils.isErrorType(type) && type != PLACEHOLDER_FUNCTION_TYPE))) {
    hasErrorInConstrainingTypes=true;
    return true;
  }
  return false;
}
