{
  if (upperConstraintKind == EQUAL || typeParameterVariance == INVARIANT) {
    return EQUAL;
  }
  if ((upperConstraintKind == SUB_TYPE && typeParameterVariance == OUT_VARIANCE) || (upperConstraintKind == SUPER_TYPE && typeParameterVariance == IN_VARIANCE)) {
    return SUB_TYPE;
  }
  return SUPER_TYPE;
}
