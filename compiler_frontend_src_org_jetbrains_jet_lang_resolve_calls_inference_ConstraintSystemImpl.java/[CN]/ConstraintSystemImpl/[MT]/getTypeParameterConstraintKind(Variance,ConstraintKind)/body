{
  if (upperConstraintKind == EQUAL || typeParameterVariance == INVARIANT) {
    return EQUAL;
  }
  if ((upperConstraintKind == SUPER_TYPE && typeParameterVariance == OUT_VARIANCE) || (upperConstraintKind == SUB_TYPE && typeParameterVariance == IN_VARIANCE)) {
    return SUPER_TYPE;
  }
  return SUB_TYPE;
}
