{
  if (standardLibrary.getArray().equals(jetType.getConstructor().getDeclarationDescriptor()))   return knowArrayTypes.get(jetType.getArguments().get(0).getType());
  return knowTypes.get(jetType);
}
