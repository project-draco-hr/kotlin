{
  final String common=AstUtil.joinNodes(myImports,N) + N2 + AstUtil.joinNodes(myClasses,N)+ N;
  if (myPackageName.isEmpty())   return common;
  return "namespace" + SPACE + myPackageName+ SPACE+ "{"+ N+ common+ "}";
}
