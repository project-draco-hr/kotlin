{
switch (initState) {
case INITIALIZED:
    return isDeclared ? VS_IT : VS_IF;
case INITIALIZED_EXHAUSTIVELY:
  return isDeclared ? VS_ET : VS_EF;
case UNKNOWN:
return isDeclared ? VS_UT : VS_UF;
default :
return isDeclared ? VS_NT : VS_NF;
}
}
