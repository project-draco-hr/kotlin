{
  ResolveSession resolveSession=getLazyResolveSessionForFile(file);
  return expression != null ? LazyBindingContextUtils.getExpressionBindingContext(resolveSession,expression) : resolveSession.getBindingContext();
}
