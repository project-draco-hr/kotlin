{
  ResolveSession resolveSession=getLazyResolveSessionForFile(file);
  return expression != null ? ResolveSessionUtils.getExpressionBindingContext(resolveSession,expression) : resolveSession.getBindingContext();
}
