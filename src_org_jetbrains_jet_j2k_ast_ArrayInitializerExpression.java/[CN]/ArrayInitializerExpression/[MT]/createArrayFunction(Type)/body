{
  String sType=innerTypeStr(type);
  if (!sType.equals("any") && PRIMITIVE_TYPES.contains(sType))   return sType + "Array";
  return AstUtil.lowerFirstCharacter(type.convertedToNotNull().toKotlin());
}
