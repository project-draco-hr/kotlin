{
  JetImportDirective importDirective=PsiTreeUtil.findChildOfType(JetPsiFactoryKt.JetPsiFactory(getProject()).createFile(text),JetImportDirective.class);
  assertNotNull("At least one import directive is expected",importDirective);
  return importDirective.getImportPath();
}
