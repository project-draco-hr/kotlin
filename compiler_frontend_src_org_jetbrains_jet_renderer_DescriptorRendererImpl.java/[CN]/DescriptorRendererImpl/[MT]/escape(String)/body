{
switch (textFormat) {
case PLAIN:
    return string;
case HTML:
  return string.replaceAll("<","&lt;").replaceAll(">","&gt;");
}
throw new IllegalStateException("Unexpected textFormat: " + textFormat);
}
