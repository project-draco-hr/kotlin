{
  if (isNativeObject(descriptor)) {
    return new NativePropertyAccessTranslator(descriptor,null,context);
  }
 else {
    return new KotlinPropertyAccessTranslator(descriptor,null,resolvedCall,context);
  }
}
