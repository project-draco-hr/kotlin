{
  if (resolverScopeData.psiClass != null) {
    @SuppressWarnings("ConstantConditions") Builder builder=new Builder(new PsiClassWrapper(resolverScopeData.psiClass),resolverScopeData.staticMembers,resolverScopeData.kotlin);
    builder.run();
    return builder.namedMembersMap;
  }
 else {
    return Collections.emptyMap();
  }
}
