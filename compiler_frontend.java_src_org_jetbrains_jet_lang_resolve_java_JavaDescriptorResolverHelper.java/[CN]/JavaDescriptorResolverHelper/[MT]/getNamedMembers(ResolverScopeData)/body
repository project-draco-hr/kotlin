{
  if (resolverScopeData.getPsiClass() != null) {
    @SuppressWarnings("ConstantConditions") Builder builder=new Builder(new PsiClassWrapper(resolverScopeData.getPsiClass()),resolverScopeData.isStaticMembers(),resolverScopeData.isKotlin());
    builder.run();
    return builder.namedMembersMap;
  }
 else {
    return Collections.emptyMap();
  }
}
