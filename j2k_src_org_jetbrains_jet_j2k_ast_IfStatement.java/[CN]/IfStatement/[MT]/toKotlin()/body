{
  String result="if" + SPACE + "("+ myCondition.toKotlin()+ ")"+ N+ myThenStatement.toKotlin()+ N;
  if (myElseStatement != Statement.EMPTY_STATEMENT)   return result + "else" + N+ myElseStatement.toKotlin();
  return result;
}
