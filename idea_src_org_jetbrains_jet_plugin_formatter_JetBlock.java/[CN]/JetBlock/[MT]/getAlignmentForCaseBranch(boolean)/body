{
  if (shouldAlignInColumns) {
    return ASTAlignmentStrategy.fromTypes(AlignmentStrategy.createAlignmentPerTypeStrategy(Arrays.asList((IElementType)ARROW),WHEN_ENTRY,true));
  }
 else {
    return ASTAlignmentStrategy.getNullStrategy();
  }
}
