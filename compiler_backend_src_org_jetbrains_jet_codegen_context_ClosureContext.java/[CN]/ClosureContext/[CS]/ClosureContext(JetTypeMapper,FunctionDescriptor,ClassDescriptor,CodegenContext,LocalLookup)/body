{
  super(contextDescriptor,OwnerKind.IMPLEMENTATION,parentContext,typeMapper.getBindingContext().get(CLOSURE,classDescriptor),classDescriptor,localLookup);
  this.classDescriptor=classDescriptor;
  initOuterExpression(typeMapper,classDescriptor);
}
