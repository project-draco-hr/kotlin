{
  if (!noExpectedType(expectedType) && !semanticServices.getTypeChecker().isSubtypeOf(nativeType,expectedType)) {
    return new ErrorValue("A " + title + " literal "+ text+ " does not conform to the expected type "+ expectedType);
  }
  return null;
}
