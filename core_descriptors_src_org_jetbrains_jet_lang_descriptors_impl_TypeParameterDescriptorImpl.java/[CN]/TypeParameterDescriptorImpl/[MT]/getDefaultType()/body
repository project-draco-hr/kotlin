{
  if (defaultType == null) {
    defaultType=new JetTypeImpl(Collections.<AnnotationDescriptor>emptyList(),getTypeConstructor(),TypeUtils.hasNullableLowerBound(this),Collections.<TypeProjection>emptyList(),new LazyScopeAdapter(new NotNullLazyValueImpl<JetScope>(){
      @NotNull @Override protected JetScope doCompute(){
        return getUpperBoundsAsType().getMemberScope();
      }
    }
));
  }
  return defaultType;
}
