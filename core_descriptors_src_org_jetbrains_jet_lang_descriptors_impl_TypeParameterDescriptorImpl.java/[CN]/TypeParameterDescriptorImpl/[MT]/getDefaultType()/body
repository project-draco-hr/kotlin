{
  if (defaultType == null) {
    defaultType=new JetTypeImpl(Collections.<AnnotationDescriptor>emptyList(),getTypeConstructor(),TypeUtils.hasNullableLowerBound(this),Collections.<TypeProjection>emptyList(),new LazyScopeAdapter(NO_LOCKS.createLazyValue(new Computable<JetScope>(){
      @Override public JetScope compute(){
        return getUpperBoundsAsType().getMemberScope();
      }
    }
)));
  }
  return defaultType;
}
