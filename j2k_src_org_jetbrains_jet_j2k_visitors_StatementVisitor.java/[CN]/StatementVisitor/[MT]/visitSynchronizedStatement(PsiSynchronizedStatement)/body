{
  super.visitSynchronizedStatement(statement);
  myResult=new SynchronizedStatement(getConverter().expressionToExpression(statement.getLockExpression()),getConverter().blockToBlock(statement.getBody()));
}
