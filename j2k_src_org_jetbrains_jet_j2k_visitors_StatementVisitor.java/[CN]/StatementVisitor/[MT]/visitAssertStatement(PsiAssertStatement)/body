{
  super.visitAssertStatement(statement);
  myResult=new AssertStatement(getConverter().expressionToExpression(statement.getAssertCondition()),getConverter().expressionToExpression(statement.getAssertDescription()));
}
