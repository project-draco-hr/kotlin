{
  MagicInstruction instruction=MagicInstruction.object$.create(instructionElement,valueElement,getCurrentScope(),synthetic,inputValues,valueFactory);
  add(instruction);
  return instruction.getOutputValue();
}
