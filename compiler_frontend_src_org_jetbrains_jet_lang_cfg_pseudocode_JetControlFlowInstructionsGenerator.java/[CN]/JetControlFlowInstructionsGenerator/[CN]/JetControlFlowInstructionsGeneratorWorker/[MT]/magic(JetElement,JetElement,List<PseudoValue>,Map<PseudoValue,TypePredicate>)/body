{
  MagicInstruction instruction=MagicInstruction.object$.create(instructionElement,valueElement,getCurrentScope(),inputValues,expectedTypes,valueFactory);
  add(instruction);
  return instruction;
}
