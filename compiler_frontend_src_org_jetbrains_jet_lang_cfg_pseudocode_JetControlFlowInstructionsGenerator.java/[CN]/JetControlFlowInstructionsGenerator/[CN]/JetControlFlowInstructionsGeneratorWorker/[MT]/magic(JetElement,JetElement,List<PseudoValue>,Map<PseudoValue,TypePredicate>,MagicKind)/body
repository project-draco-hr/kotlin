{
  MagicInstruction instruction=MagicInstruction.object$.create(instructionElement,valueElement,getCurrentScope(),inputValues,expectedTypes,kind,valueFactory);
  add(instruction);
  return instruction;
}
