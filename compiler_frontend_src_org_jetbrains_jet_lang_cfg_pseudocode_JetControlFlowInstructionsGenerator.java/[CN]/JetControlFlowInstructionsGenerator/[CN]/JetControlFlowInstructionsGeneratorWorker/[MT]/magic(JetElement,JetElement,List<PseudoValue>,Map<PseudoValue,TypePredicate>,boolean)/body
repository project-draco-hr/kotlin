{
  MagicInstruction instruction=MagicInstruction.object$.create(instructionElement,valueElement,getCurrentScope(),synthetic,inputValues,expectedTypes,valueFactory);
  add(instruction);
  return instruction;
}
