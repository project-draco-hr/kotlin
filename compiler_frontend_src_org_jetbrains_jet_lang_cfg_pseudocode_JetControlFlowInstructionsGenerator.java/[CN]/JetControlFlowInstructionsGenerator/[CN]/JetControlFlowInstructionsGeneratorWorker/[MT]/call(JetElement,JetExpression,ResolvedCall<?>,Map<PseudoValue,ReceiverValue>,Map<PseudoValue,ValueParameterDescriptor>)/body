{
  JetType returnType=resolvedCall.getResultingDescriptor().getReturnType();
  CallInstruction instruction=CallInstruction.object$.create(instructionElement,valueElement,getCurrentScope(),resolvedCall,receiverValues,arguments,returnType != null && KotlinBuiltIns.getInstance().isNothing(returnType) ? null : valueFactory);
  add(instruction);
  return instruction;
}
