{
  JetType returnType=resolvedCall.getResultingDescriptor().getReturnType();
  OperationInstruction instruction=CallInstruction.object$.create(expression,getCurrentScope(),resolvedCall,receiverValues,arguments,returnType != null && KotlinBuiltIns.getInstance().isNothing(returnType) ? null : valueFactory);
  add(instruction);
  return instruction.getOutputValue();
}
