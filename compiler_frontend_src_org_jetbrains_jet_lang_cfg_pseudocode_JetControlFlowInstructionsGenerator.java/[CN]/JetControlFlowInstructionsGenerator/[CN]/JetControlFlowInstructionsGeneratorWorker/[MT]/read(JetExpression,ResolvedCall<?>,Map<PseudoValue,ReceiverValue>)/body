{
  AccessTarget accessTarget=resolvedCall != null ? new AccessTarget.Call(resolvedCall) : AccessTarget.BlackBox.instance$;
  ReadValueInstruction instruction=ReadValueInstruction.object$.create(expression,getCurrentScope(),accessTarget,receiverValues,valueFactory);
  add(instruction);
  return instruction.getOutputValue();
}
