{
  FunctionDescriptor functionDescriptor=context.services.callResolver.resolveCallWithGivenName(scope,CallMaker.makeCall(binaryExpression),binaryExpression.getOperationReference(),name,binaryExpression.getLeft(),context.expectedType);
  if (functionDescriptor != null) {
    return functionDescriptor.getReturnType();
  }
  return null;
}
