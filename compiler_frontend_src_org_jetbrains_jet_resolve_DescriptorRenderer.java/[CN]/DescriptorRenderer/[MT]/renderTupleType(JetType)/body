{
  StringBuilder sb=new StringBuilder("#(");
  appendTypes(sb,JetStandardClasses.getTupleElementTypes(type));
  sb.append(")");
  if (type.isNullable()) {
    sb.append("?");
  }
  return sb.toString();
}
