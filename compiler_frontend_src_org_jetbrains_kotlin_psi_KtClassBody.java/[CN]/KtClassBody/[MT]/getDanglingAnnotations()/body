{
  return CollectionsKt.flatMap(getStubOrPsiChildrenAsList(MODIFIER_LIST),new Function1<KtModifierList,Iterable<KtAnnotationEntry>>(){
    @Override public Iterable<KtAnnotationEntry> invoke(    KtModifierList modifierList){
      return modifierList.getAnnotationEntries();
    }
  }
);
}
