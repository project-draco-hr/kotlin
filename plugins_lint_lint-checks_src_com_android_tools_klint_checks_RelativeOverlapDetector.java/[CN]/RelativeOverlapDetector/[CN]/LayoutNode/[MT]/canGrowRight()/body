{
  Set<LayoutNode> nodes;
  if (mToLeft != null) {
    nodes=mToLeft.canGrowRight();
  }
 else {
    nodes=new LinkedHashSet<LayoutNode>();
  }
  if (!fixedWidth()) {
    nodes.add(this);
  }
  return nodes;
}
