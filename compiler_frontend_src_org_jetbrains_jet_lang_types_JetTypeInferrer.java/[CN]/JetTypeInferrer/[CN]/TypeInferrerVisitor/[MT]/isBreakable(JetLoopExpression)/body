{
  final boolean[] result=new boolean[1];
  result[0]=false;
  expression.accept(new JetTreeVisitorVoid(){
    @Override public void visitBreakExpression(    JetBreakExpression expression){
      result[0]=true;
    }
  }
);
  return result[0];
}
