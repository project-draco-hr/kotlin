{
  while (!eof()) {
    if (at(RBRACE)) {
      break;
    }
    if (deprecatedEnumEntryPossible && parseEnumEntry()) {
      parseEnumEntryDelimiter();
      consumeIf(SEMICOLON);
    }
 else {
      parseMemberDeclaration();
    }
  }
}
