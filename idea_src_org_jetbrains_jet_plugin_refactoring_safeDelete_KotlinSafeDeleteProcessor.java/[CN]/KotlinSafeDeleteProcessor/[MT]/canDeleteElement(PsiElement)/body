{
  if (PsiUtilPackage.isObjectLiteral(element))   return false;
  return element instanceof JetClassOrObject || element instanceof JetNamedFunction || element instanceof PsiMethod|| element instanceof JetProperty|| element instanceof JetTypeParameter|| element instanceof JetParameter;
}
