{
  return callResolvePerfCounter.time(new Function0<OverloadResolutionResults<F>>(){
    @Override public OverloadResolutionResults<F> invoke(){
      List<ResolutionTask<D,F>> prioritizedTasks=taskPrioritizer.<D,F>computePrioritizedTasksFromCandidates(context,candidates,tracing);
      return doResolveCallOrGetCachedResults(context,prioritizedTasks,callTransformer,tracing);
    }
  }
);
}
