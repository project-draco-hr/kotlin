{
  CapturedParamInfo capturedParamInfo=new CapturedParamInfo(fieldName,type,skipped,original != null ? original.getIndex() : -1,nextCaptured);
  additionalCapturedParams.add(capturedParamInfo);
  return capturedParamInfo;
}
