{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  IntRange range=(IntRange)o;
  return count == range.count && start == range.start;
}
