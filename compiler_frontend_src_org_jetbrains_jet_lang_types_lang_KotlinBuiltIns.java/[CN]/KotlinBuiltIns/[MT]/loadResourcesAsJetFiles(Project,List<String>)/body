{
  List<JetFile> files=new LinkedList<JetFile>();
  for (  String path : libraryFiles) {
    String text=loadBuiltInFileText(path);
    JetFile file=(JetFile)PsiFileFactory.getInstance(project).createFileFromText(path,JetFileType.INSTANCE,StringUtil.convertLineSeparators(text),LocalTimeCounter.currentTime(),true,false);
    files.add(file);
  }
  return files;
}
