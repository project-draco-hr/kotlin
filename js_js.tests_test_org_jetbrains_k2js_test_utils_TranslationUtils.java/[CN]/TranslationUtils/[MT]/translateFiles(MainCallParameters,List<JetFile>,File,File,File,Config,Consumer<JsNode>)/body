{
  OutputFileCollection outputFiles=translateWithMainCallParameters(mainCall,files,outputFile,outputPrefixFile,outputPostfixFile,config,astConsumer);
  OutputUtilsPackage.writeAllTo(outputFiles,outputFile.getParentFile());
}
