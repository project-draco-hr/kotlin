{
  if (from.isEmpty())   return Collections.emptyList();
  return CollectionsKt.filterNotNull(CollectionsKt.map(from,new Function1<JetElement,PseudoValue>(){
    @Override public PseudoValue invoke(    JetElement element){
      return getBoundOrUnreachableValue(element);
    }
  }
));
}
