{
  if (from.isEmpty())   return Collections.emptyList();
  return CollectionsKt.filterNotNull(CollectionsKt.map(from,new Function1<KtElement,PseudoValue>(){
    @Override public PseudoValue invoke(    KtElement element){
      return getBoundOrUnreachableValue(element);
    }
  }
));
}
