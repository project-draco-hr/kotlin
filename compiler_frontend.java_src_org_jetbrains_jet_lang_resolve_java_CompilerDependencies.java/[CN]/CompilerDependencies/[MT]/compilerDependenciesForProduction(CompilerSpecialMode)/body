{
  return new CompilerDependencies(compilerSpecialMode,compilerSpecialMode.includeJdk() ? PathUtil.findRtJar() : null,compilerSpecialMode.includeJdkAnnotations() ? PathUtil.getJdkAnnotationsPath() : null,compilerSpecialMode.includeKotlinRuntime() ? PathUtil.getDefaultRuntimePath() : null);
}
