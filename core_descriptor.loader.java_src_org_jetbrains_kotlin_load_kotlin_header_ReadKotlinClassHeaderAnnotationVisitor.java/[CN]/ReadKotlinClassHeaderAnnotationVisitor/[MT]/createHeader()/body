{
  if (headerKind == null) {
    return null;
  }
  if (metadataVersion == null || !metadataVersion.isCompatible()) {
    annotationData=null;
  }
 else   if (shouldHaveData() && annotationData == null) {
    return null;
  }
  return new KotlinClassHeader(headerKind,metadataVersion != null ? metadataVersion : JvmMetadataVersion.INVALID_VERSION,bytecodeVersion != null ? bytecodeVersion : JvmBytecodeBinaryVersion.INVALID_VERSION,annotationData,strings,filePartClassNames,multifileClassName,isInterfaceDefaultImpls,isLocalClass);
}
