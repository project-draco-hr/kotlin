{
  if (headerKind == null) {
    return null;
  }
  if (!AbiVersionUtil.isAbiVersionCompatible(version)) {
    annotationData=null;
  }
 else   if (shouldHaveData() && annotationData == null) {
    return null;
  }
  return new KotlinClassHeader(headerKind,version,annotationData,strings,syntheticClassKind,filePartClassNames,multifileClassName,isInterfaceDefaultImpls,isLocalClass);
}
