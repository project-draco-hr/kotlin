{
  if (headerKind == null) {
    return null;
  }
  if (!version.isCompatible()) {
    annotationData=null;
  }
 else   if (shouldHaveData() && annotationData == null) {
    return null;
  }
  return new KotlinClassHeader(headerKind,version,annotationData,strings,filePartClassNames,multifileClassName,isInterfaceDefaultImpls,isLocalClass);
}
