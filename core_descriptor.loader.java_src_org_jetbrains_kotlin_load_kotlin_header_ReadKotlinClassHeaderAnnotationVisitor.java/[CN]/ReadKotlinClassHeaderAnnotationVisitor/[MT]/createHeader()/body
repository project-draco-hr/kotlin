{
  if (headerKind == null) {
    return null;
  }
  if (headerKind == CLASS && classKind == null) {
    classKind=KotlinClass.Kind.CLASS;
  }
  if (!AbiVersionUtil.isAbiVersionCompatible(version)) {
    annotationData=null;
  }
 else   if (shouldHaveData() && annotationData == null) {
    return null;
  }
  return new KotlinClassHeader(headerKind,version,annotationData,strings,classKind,syntheticClassKind,filePartClassNames,multifileClassName,isInterfaceDefaultImpls);
}
