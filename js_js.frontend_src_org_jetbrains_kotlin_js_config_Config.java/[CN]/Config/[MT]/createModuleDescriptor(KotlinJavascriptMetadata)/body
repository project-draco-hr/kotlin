{
  ModuleDescriptorImpl moduleDescriptor=new ModuleDescriptorImpl(Name.special("<" + metadata.getModuleName() + ">"),storageManager,TopDownAnalyzerFacadeForJS.JS_MODULE_PARAMETERS);
  PackageFragmentProvider provider=KotlinJavascriptSerializationUtil.createPackageFragmentProvider(moduleDescriptor,metadata.getBody(),storageManager);
  moduleDescriptor.initialize(provider != null ? provider : PackageFragmentProvider.Empty.INSTANCE$);
  return moduleDescriptor;
}
