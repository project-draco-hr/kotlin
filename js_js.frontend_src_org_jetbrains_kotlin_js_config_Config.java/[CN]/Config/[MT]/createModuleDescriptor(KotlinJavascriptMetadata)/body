{
  ModuleDescriptorImpl moduleDescriptor=TopDownAnalyzerFacadeForJS.createJsModule("<" + metadata.getModuleName() + ">");
  PackageFragmentProvider provider=KotlinJavascriptSerializationUtil.createPackageFragmentProvider(moduleDescriptor,metadata.getBody());
  moduleDescriptor.initialize(provider != null ? provider : new CompositePackageFragmentProvider(Collections.<PackageFragmentProvider>emptyList()));
  moduleDescriptor.addDependencyOnModule(KotlinBuiltIns.getInstance().getBuiltInsModule());
  return moduleDescriptor;
}
