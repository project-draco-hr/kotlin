{
  if (mTargets == null) {
    SdkWrapper localSdk=getSdk();
    if (localSdk != null) {
      mTargets=localSdk.getTargets();
    }
 else {
      mTargets=new IAndroidTarget[0];
    }
  }
  return mTargets;
}
