{
  JetTypeReference extendsBound=psi.getExtendsBound();
  return new PsiJetTypeParameterStubImpl(parentStub,psi.getName(),extendsBound != null ? extendsBound.getText() : null,psi.getVariance() == Variance.IN_VARIANCE,psi.getVariance() == Variance.OUT_VARIANCE);
}
