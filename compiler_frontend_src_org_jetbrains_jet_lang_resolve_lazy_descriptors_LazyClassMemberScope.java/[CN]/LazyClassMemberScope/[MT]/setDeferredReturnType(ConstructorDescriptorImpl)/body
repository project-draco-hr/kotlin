{
  descriptor.setReturnType(DeferredType.create(resolveSession.getStorageManager(),trace,new Function0<JetType>(){
    @Override public JetType invoke(){
      return thisDescriptor.getDefaultType();
    }
  }
));
}
