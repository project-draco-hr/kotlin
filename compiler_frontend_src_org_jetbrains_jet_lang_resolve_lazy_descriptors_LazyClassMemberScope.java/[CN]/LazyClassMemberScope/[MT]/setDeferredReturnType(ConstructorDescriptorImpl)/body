{
  descriptor.setReturnType(DeferredType.create(resolveSession.getStorageManager(),resolveSession.getTrace(),new Function0<JetType>(){
    @Override public JetType invoke(){
      return thisDescriptor.getDefaultType();
    }
  }
));
}
