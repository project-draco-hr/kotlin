{
  descriptor.setReturnType(DeferredType.create(resolveSession.getTrace(),resolveSession.getStorageManager().createLazyValue(new Function0<JetType>(){
    @Override public JetType invoke(){
      return thisDescriptor.getDefaultType();
    }
  }
)));
}
