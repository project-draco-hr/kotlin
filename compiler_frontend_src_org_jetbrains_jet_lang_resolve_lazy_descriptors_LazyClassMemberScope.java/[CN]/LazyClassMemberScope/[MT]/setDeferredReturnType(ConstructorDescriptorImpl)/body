{
  descriptor.setReturnType(DeferredType.create(resolveSession.getTrace(),resolveSession.getStorageManager().createLazyValue(new Computable<JetType>(){
    @Override public JetType compute(){
      return thisDescriptor.getDefaultType();
    }
  }
)));
}
