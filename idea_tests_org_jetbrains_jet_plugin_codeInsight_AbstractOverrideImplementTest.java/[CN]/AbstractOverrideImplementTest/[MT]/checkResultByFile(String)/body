{
  try {
    myFixture.checkResultByFile(fileName);
  }
 catch (  AssertionError error) {
    JetTestUtils.assertEqualsToFile(new File(myFixture.getTestDataPath(),fileName),TagsTestDataUtil.generateTextWithCaretAndSelection(myFixture.getEditor()));
  }
}
