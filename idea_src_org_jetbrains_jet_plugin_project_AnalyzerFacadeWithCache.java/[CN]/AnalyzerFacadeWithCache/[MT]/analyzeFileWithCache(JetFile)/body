{
synchronized (lock) {
    Project project=file.getProject();
    return CachedValuesManager.getManager(project).getCachedValue(project,ANALYZE_EXHAUST_FULL,new SLRUCachedAnalyzeExhaustProvider(),false).get(file);
  }
}
