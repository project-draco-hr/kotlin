{
  return new Function<JetFile,List<JetFile>>(){
    @Override public List<JetFile> fun(    JetFile file){
      return new SameJetFilePredicate(file).filter(sampleToAllFilesInModule().fun(file));
    }
  }
;
}
