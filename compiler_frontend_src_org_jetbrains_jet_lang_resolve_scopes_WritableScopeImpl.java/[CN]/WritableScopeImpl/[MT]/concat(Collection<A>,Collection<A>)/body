{
  if (c1.isEmpty()) {
    return c2;
  }
 else   if (c2.isEmpty()) {
    return c1;
  }
 else {
    Collection<A> r=Lists.newArrayListWithCapacity(c1.size() + c2.size());
    r.addAll(c1);
    r.addAll(c2);
    return r;
  }
}
