{
  this(context,TracingStrategyImpl.create(reference,context.call),new Function0<Collection<ResolutionCandidate<D>>>(){
    @Override public Collection<ResolutionCandidate<D>> invoke(){
      return candidates;
    }
  }
);
}
