{
  String prefix="";
  if (severity == EXCEPTION) {
    prefix=CompilerRunnerConstants.INTERNAL_ERROR_PREFIX;
  }
  context.processMessage(new CompilerMessage(CompilerRunnerConstants.KOTLIN_COMPILER_NAME,kind(severity),prefix + message,location.getPath(),-1,-1,-1,location.getLine(),location.getColumn()));
}
