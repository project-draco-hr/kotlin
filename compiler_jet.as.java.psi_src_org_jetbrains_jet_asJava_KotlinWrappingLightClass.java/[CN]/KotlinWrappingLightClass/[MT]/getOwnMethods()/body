{
  return ContainerUtil.map(getDelegate().getMethods(),new Function<PsiMethod,PsiMethod>(){
    @Override public PsiMethod fun(    PsiMethod method){
      return new LightMethod(myManager,method,KotlinWrappingLightClass.this);
    }
  }
);
}
