{
  compileKotlinToJvm("built-ins",destDir,UtilsPackage.join(Arrays.asList(PATHS),File.pathSeparator),PATHS);
  JetTestUtils.compileJavaFiles(javaFilesUnder(PATHS),Arrays.asList("-classpath",destDir.getPath(),"-d",destDir.getPath()));
}
