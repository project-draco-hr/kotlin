{
  return KotlinPackage.flatMap(Arrays.asList(paths),new Function1<String,List<File>>(){
    @Override public List<File> invoke(    String path){
      return FileUtil.findFilesByMask(Pattern.compile(".*\\.java"),new File(path));
    }
  }
);
}
