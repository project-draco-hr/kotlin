{
  KotlinCompiler.ExitCode exitCode=new KotlinCompiler().exec(System.err,"-output",classesDir.getPath(),"-src","./jdk-headers/src","-mode","jdkHeaders");
  if (exitCode != KotlinCompiler.ExitCode.OK) {
    throw new IllegalStateException("jdk headers compilation failed: " + exitCode);
  }
}
