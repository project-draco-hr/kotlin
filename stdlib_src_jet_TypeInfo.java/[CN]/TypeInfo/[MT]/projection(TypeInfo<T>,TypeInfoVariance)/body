{
switch (variance) {
case IN:
    return inProjection(typeInfo);
case OUT:
  return outProjection(typeInfo);
case INVARIANT:
return invariantProjection(typeInfo);
default :
throw new IllegalStateException();
}
}
