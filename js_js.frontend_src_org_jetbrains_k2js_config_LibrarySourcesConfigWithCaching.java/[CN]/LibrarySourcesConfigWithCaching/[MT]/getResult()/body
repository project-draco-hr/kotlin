{
  if (result == null) {
    result=TopDownAnalyzerFacadeForJS.analyzeFiles(generateLibFiles(),Predicates.<PsiFile>alwaysFalse(),createConfigWithoutLibFiles(getProject(),getModuleId(),getTarget()));
  }
  return result;
}
