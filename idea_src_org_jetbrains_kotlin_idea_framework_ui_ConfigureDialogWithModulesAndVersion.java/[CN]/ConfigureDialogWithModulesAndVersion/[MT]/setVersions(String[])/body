{
  Collection<String> items;
  try {
    items=loadVersions();
    hideLoader();
  }
 catch (  Throwable t) {
    items=Arrays.asList(kotlinVersions);
    showWarning();
  }
  updateVersions(items);
}
