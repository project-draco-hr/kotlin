{
  PsiBuilder.Marker body=mark();
  if (at(SEMICOLON)) {
    PsiBuilder.Marker mark=mark();
    advance();
    mark.done(EMPTY_EXPRESSION);
  }
 else {
    parseExpression();
  }
  body.done(BODY);
}
