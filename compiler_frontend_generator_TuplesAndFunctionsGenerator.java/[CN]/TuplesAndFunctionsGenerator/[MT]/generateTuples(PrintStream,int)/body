{
  generated(out);
  out.println("class Tuple0() {}");
  for (int i=1; i < count; i++) {
    out.print("class Tuple" + i);
    out.print("<");
    for (int j=1; j <= i; j++) {
      out.print("out T" + j);
      if (j < i) {
        out.print(", ");
      }
    }
    out.print(">");
    out.print("(");
    for (int j=1; j <= i; j++) {
      out.print("_" + j + ": "+ "T"+ j);
      if (j < i) {
        out.print(", ");
      }
    }
    out.print(") {}");
    out.println();
  }
}
