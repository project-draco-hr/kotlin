{
  if (preserveOriginal) {
    return new DeserializedSimpleFunctionDescriptor(newOwner,getOriginal(),getAnnotations(),getName(),kind,functionProto,nameResolver);
  }
 else {
    return new DeserializedSimpleFunctionDescriptor(newOwner,getAnnotations(),getName(),kind,functionProto,nameResolver);
  }
}
