{
  if (!hasFormals) {
    hasFormals=true;
    buf.append('<');
  }
switch (variance) {
case OUT_VARIANCE:
    buf.append("out ");
  break;
case IN_VARIANCE:
buf.append("in ");
break;
case INVARIANT:
break;
default :
throw new IllegalStateException();
}
buf.append(name);
buf.append(':');
}
