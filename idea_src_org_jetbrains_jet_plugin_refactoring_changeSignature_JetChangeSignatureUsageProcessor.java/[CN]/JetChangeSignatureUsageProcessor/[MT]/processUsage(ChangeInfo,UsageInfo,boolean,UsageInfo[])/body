{
  if (beforeMethodChange)   return true;
  PsiElement element=usageInfo.getElement();
  if (element != null)   return usageInfo instanceof JetUsageInfo ? ((JetUsageInfo)usageInfo).processUsage((JetChangeInfo)changeInfo,element) : true;
 else   return false;
}
