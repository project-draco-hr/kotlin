{
  JetExpression body=element.getBodyExpression();
  if (body != null) {
    for (    PsiElement child : body.getChildren()) {
      if (child instanceof JetExpression)       return bindingContext.get(BindingContext.RESOLUTION_SCOPE,(JetExpression)child);
    }
  }
  return null;
}
