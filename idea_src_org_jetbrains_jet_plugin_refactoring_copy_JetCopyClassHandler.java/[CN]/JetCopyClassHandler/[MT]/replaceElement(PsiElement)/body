{
  if (element instanceof JetClassOrObject && JetMoveFilesOrDirectoriesHandler.isMovableClass((JetClassOrObject)element)) {
    return element.getContainingFile();
  }
 else {
    return element;
  }
}
