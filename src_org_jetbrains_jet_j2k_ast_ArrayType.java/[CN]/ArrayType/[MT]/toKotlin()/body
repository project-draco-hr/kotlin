{
  String nullable=isNullableStr();
  if (PRIMITIVE_TYPES.contains(myType.toKotlin().toLowerCase()))   return myType.toKotlin() + "Array" + nullable;
  return "Array" + "<" + myType.toKotlin() + ">"+ nullable;
}
