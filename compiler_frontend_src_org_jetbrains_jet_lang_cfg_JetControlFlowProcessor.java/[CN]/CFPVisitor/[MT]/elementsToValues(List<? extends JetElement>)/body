{
  return KotlinPackage.filterNotNull(KotlinPackage.mapTo(from,new LinkedHashSet<PseudoValue>(),new Function1<JetElement,PseudoValue>(){
    @Override public PseudoValue invoke(    JetElement element){
      return builder.getBoundValue(element);
    }
  }
));
}
