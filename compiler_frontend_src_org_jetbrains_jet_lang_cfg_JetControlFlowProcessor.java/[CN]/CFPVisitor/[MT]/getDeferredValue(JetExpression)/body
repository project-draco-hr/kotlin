{
  return new Function0<PseudoValue>(){
    @Override public PseudoValue invoke(){
      generateInstructions(expression,NOT_IN_CONDITION);
      return builder.getBoundValue(expression);
    }
  }
;
}
