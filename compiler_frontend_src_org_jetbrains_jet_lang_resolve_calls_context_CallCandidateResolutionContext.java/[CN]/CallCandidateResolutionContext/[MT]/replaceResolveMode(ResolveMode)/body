{
  if (newResolveMode == resolveMode)   return this;
  return new CallCandidateResolutionContext<D>(candidateCall,tracing,trace,scope,call,expectedType,dataFlowInfo,newResolveMode,expressionPosition);
}
