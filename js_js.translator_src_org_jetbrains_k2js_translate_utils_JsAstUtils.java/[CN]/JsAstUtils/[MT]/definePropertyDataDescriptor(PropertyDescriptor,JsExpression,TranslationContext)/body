{
  return AstUtil.newInvocation(DEFINE_PROPERTY,new JsThisRef(),context.program().getStringLiteral(context.getNameForDescriptor(descriptor).getIdent()),createPropertyDataDescriptor(descriptor.isVar(),descriptor,value,context));
}
