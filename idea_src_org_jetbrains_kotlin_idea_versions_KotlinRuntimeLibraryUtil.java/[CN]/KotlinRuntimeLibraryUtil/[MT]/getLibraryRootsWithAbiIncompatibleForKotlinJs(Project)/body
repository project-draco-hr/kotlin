{
  return getLibraryRootsWithAbiIncompatibleVersion(project,KotlinJavaScriptAbiVersionIndex.INSTANCE$,new Function1<Module,Boolean>(){
    @Override public Boolean invoke(    @Nullable Module module){
      return module != null && ProjectStructureUtil.isJsKotlinModule(module);
    }
  }
,new Function1<Integer,Boolean>(){
    @Override public Boolean invoke(    Integer abiVersion){
      return !KotlinJavascriptMetadataUtils.isAbiVersionCompatible(abiVersion);
    }
  }
);
}
