{
  return getLibraryRootsWithAbiIncompatibleVersion(project,KotlinMetadataVersionIndex.INSTANCE,new Function1<Module,Boolean>(){
    @Override public Boolean invoke(    @Nullable Module module){
      return module != null && ProjectStructureUtil.isJavaKotlinModule(module);
    }
  }
,new Function1<BinaryVersion,Boolean>(){
    @Override public Boolean invoke(    @NotNull BinaryVersion version){
      return !version.isCompatible();
    }
  }
);
}
