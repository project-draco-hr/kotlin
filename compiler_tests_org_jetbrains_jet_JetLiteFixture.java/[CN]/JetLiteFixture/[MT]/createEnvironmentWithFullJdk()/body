{
  myEnvironment=new JetCoreEnvironment(getTestRootDisposable(),CompileCompilerDependenciesTest.compilerDependenciesForTests(CompilerSpecialMode.REGULAR));
  final File rtJar=CompileEnvironmentUtil.findRtJar();
  myEnvironment.addToClasspath(rtJar);
}
