{
  return CollectionsKt.singleOrNull(parameterInfos,new Function1<ParameterInfo,Boolean>(){
    @Override public Boolean invoke(    ParameterInfo info){
      return info.isEnabled() && info.isReceiver();
    }
  }
);
}
