{
  return analyzeAndGenerate(configuration,configuration.getEnvironment().getConfiguration().get(JVMConfigurationKeys.STUBS,false),configuration.getEnvironment().getConfiguration().getList(JVMConfigurationKeys.SCRIPT_PARAMETERS));
}
