{
  KotlinClassStub stub=getStub();
  if (stub != null) {
    return stub.isInterface();
  }
  return findChildByType(JetTokens.TRAIT_KEYWORD) != null || findChildByType(JetTokens.INTERFACE_KEYWORD) != null;
}
