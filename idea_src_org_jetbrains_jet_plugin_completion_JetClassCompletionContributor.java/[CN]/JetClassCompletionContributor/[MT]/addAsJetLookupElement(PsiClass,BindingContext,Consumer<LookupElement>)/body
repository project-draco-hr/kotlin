{
  if (aClass instanceof JetLightClass) {
    ClassDescriptor descriptor=bindingContext.get(BindingContext.FQNAME_TO_CLASS_DESCRIPTOR,((JetLightClass)aClass).getFqName());
    if (descriptor != null) {
      LookupElement element=DescriptorLookupConverter.createLookupElement(bindingContext,descriptor);
      consumer.consume(element);
      return true;
    }
  }
  return false;
}
