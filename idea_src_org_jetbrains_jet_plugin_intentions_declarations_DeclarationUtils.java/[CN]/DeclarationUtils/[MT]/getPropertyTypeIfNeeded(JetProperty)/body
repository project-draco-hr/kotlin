{
  if (property.getTypeReference() != null)   return null;
  JetType type=AnalyzerFacadeWithCache.getContextForElement(property).get(BindingContext.EXPRESSION_TYPE,property.getInitializer());
  return type == null || type.isError() ? null : type;
}
