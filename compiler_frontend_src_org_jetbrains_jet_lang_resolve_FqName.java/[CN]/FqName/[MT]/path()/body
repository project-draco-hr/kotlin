{
  final List<FqName> path=Lists.newArrayList();
  path.add(ROOT);
  fqName.walk(new FqNameUnsafe.WalkCallback(){
    @Override public void segment(    @NotNull String shortName,    @NotNull FqNameUnsafe fqName){
      path.add(new FqName(fqName));
    }
  }
);
  return path;
}
