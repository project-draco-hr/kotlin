{
  final List<FqName> path=Lists.newArrayList();
  path.add(ROOT);
  walk(new WalkCallback(){
    @Override public void segment(    @NotNull String shortName,    @NotNull FqName fqName){
      path.add(fqName);
    }
  }
);
  return path;
}
