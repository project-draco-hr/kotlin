{
  CompletionSorter sorter=CompletionSorter.defaultSorter(parameters,result.getPrefixMatcher());
  sorter=sorter.weighAfter("negativeStats",new JetLocalPreferableWeigher(),new JetExplicitlyImportedWeigher((JetFile)parameters.getOriginalFile()));
  return result.withRelevanceSorter(sorter);
}
