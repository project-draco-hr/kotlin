{
  return new HashSet<BoxedBasicValue>(Collections2.filter(values,new Predicate<BoxedBasicValue>(){
    @Override public boolean apply(    BoxedBasicValue input){
      return input.isSafeToRemove();
    }
  }
));
}
