{
  return LockBasedStorageManager.NO_LOCKS.createLazyValueWithPostCompute(compute,new Function<Boolean,Object>(){
    @Override public Object fun(    Boolean firstTime){
      if (firstTime)       return WrappedValues.escapeThrowable(new ReenteringLazyValueComputationException());
      return defaultValue;
    }
  }
,Consumer.EMPTY_CONSUMER);
}
