{
  return NO_LOCKS.createLazyValueWithPostCompute(compute,new Function<Boolean,T>(){
    @Override public T fun(    Boolean firstTime){
      if (firstTime)       throw new ReenteringLazyValueComputationException();
      return defaultValue;
    }
  }
,Consumer.EMPTY_CONSUMER);
}
