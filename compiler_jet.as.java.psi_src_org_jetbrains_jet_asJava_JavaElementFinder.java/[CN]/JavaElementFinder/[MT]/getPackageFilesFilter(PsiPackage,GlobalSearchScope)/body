{
  return new Predicate<PsiFile>(){
    @Override public boolean apply(    @Nullable PsiFile input){
      if (input instanceof JetFile && !(psiPackage.getQualifiedName().equals(((JetFile)input).getPackageFqName().asString()))) {
        return false;
      }
      return true;
    }
  }
;
}
