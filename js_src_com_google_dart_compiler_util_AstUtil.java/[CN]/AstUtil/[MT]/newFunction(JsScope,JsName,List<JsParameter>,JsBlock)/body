{
  JsFunction fn=new JsFunction(scope);
  if (name != null) {
    fn.setName(name);
  }
  if (params != null) {
    for (    JsParameter param : params) {
      fn.getParameters().add(param);
    }
  }
  fn.setBody(body);
  return fn;
}
