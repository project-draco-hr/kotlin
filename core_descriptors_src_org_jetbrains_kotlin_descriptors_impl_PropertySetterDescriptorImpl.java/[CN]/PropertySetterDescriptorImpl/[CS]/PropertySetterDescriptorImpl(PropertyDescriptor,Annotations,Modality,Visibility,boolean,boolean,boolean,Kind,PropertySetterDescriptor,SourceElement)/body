{
  super(modality,visibility,correspondingProperty,annotations,Name.special("<set-" + correspondingProperty.getName() + ">"),hasBody,isDefault,isExternal,kind,source);
  this.original=original != null ? original : this;
}
