{
  if (obj == null) {
    return false;
  }
  if (obj == this) {
    return true;
  }
  if (obj.getClass() != getClass()) {
    return false;
  }
  return Comparing.equal(position,((LookupPositionObject)obj).position);
}
