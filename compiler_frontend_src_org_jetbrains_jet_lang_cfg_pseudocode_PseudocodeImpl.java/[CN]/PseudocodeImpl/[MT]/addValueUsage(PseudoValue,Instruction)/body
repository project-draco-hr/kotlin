{
  if (usage instanceof MergeInstruction)   return;
  KotlinPackage.getOrPut(valueUsages,value,new Function0<List<Instruction>>(){
    @Override public List<Instruction> invoke(){
      return Lists.newArrayList();
    }
  }
).add(usage);
}
