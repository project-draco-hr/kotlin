{
switch (list.size()) {
case 0:
    throw newIndexOutOfBounds(list,index);
case 1:
  if (index == 0) {
    return Collections.singletonList(e);
  }
 else {
    throw newIndexOutOfBounds(list,index);
  }
default :
list.set(index,e);
return list;
}
}
