{
  String tag=element.getTagName();
  if (tag.equals(VIEW_TAG)) {
    return true;
  }
  return tag.indexOf('.') != -1 && (!tag.startsWith(ANDROID_PKG_PREFIX) || tag.startsWith(ANDROID_SUPPORT_PKG_PREFIX));
}
