{
  if (property.getTypeReference() != null)   return null;
  JetType type=ResolvePackage.analyze(property,BodyResolveMode.FULL).get(BindingContext.EXPRESSION_TYPE,property.getInitializer());
  return type == null || type.isError() ? null : type;
}
