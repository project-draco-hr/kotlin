{
  JsExpression jsInitExpression=null;
  JetExpression initializer=declaration.getInitializer();
  if (initializer != null) {
    jsInitExpression=AstUtil.convertToExpression((new ExpressionTranslator(context)).translate(initializer));
  }
  return jsInitExpression;
}
