{
  if (isNamespaceEmpty()) {
    return program().getEmptyStmt();
  }
  classDeclarationTranslator.generateDeclarations();
  return AstUtil.newBlock(classDeclarationsStatement(),namespaceOwnDeclarationStatement(),namespaceInitializeStatement());
}
