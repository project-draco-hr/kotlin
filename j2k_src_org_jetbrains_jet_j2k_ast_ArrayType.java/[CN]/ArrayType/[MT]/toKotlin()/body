{
  if (PRIMITIVE_TYPES.contains(myType.toKotlin().toLowerCase())) {
    return myType.toKotlin() + "Array" + isNullableStr();
  }
  return "Array" + "<" + myType.toKotlin() + ">"+ isNullableStr();
}
