{
  for (  JetNamedFunction function : functions) {
    c.getFunctions().put(function,(SimpleFunctionDescriptor)lazyDeclarationResolver.resolveToDescriptor(function));
    registerScope(c,function);
  }
}
