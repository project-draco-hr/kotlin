{
  long result=cumulative;
  if (!paused) {
    result+=System.nanoTime() - start;
  }
  paused=true;
  cumulative=0;
  OUT_LOCK.lock();
  try {
    println(name," took ",format(result));
    printStackTrace();
  }
  finally {
    OUT_LOCK.unlock();
  }
  return this;
}
