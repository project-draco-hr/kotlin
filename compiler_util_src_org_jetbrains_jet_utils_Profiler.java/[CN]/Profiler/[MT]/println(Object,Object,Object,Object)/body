{
  if (!mute) {
    OUT_LOCK.lock();
    try {
      out.print(a);
      out.print(b);
      out.print(c);
      for (      Object o : rest) {
        out.print(o);
      }
      out.println();
    }
  finally {
      OUT_LOCK.unlock();
    }
  }
  return this;
}
