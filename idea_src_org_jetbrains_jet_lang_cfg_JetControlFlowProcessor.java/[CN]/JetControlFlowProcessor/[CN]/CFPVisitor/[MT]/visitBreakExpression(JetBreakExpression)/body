{
  JetSimpleNameExpression labelElement=expression.getTargetLabel();
  Label exitPoint=(labelElement != null) ? builder.getExitPoint(resolveLabel(expression.getLabelName(),expression.getTargetLabel())) : builder.getExitPoint(builder.getCurrentLoop());
  builder.jump(exitPoint);
}
