{
  if (exhaust == null) {
    exhaust=TopDownAnalyzerFacadeForJS.analyzeFiles(generateLibFiles(),Predicates.<PsiFile>alwaysFalse(),createConfigWithoutLibFiles(getProject(),getModuleId(),getTarget()));
  }
  return exhaust;
}
