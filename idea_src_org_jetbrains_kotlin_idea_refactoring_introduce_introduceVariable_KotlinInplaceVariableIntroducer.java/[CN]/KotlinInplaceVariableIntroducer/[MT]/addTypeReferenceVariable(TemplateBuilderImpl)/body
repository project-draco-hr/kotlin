{
  JetTypeReference typeReference=myDeclaration.getTypeReference();
  if (typeReference != null) {
    builder.replaceElement(typeReference,TYPE_REFERENCE_VARIABLE_NAME,SpecifyTypeExplicitlyAction.createTypeExpressionForTemplate(myExprType),false);
  }
}
