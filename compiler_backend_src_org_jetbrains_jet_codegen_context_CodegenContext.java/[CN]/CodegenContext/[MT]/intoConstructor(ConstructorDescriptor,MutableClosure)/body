{
  if (descriptor == null) {
    descriptor=ConstructorDescriptorImpl.create(getThisDescriptor(),Annotations.EMPTY,true).initialize(Collections.<TypeParameterDescriptor>emptyList(),Collections.<ValueParameterDescriptor>emptyList(),Visibilities.PUBLIC,false);
  }
  return new ConstructorContext(descriptor,getContextKind(),this,closure);
}
