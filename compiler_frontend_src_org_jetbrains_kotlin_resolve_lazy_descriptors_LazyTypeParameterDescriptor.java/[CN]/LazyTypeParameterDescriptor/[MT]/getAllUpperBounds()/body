{
  return CollectionsKt.plus(typeParameter.getExtendsBound() != null ? Collections.singletonList(typeParameter.getExtendsBound()) : Collections.<KtTypeReference>emptyList(),getUpperBoundsFromWhereClause());
}
