{
  if (size == 0)   return this;
  if (list.contains(first))   return rest.minusAll(list);
  ConsPStack<E> newRest=rest.minusAll(list);
  if (newRest == rest)   return this;
  return new ConsPStack<E>(first,newRest);
}
