{
  if (start < 0 || end > size || start > end)   throw new IndexOutOfBoundsException();
  if (end == size)   return subList(start);
  if (start == end)   return empty();
  if (start == 0)   return new ConsPStack<E>(first,rest.subList(0,end - 1));
  return rest.subList(start - 1,end - 1);
}
