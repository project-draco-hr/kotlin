{
  super(contextDescriptor,contextKind,parentContext,null);
  final Type type=enclosingClassType(typeMapper);
  outerExpression=type != null ? StackValue.field(type,typeMapper.getClassFQName(contextDescriptor),"this$0",false) : null;
}
