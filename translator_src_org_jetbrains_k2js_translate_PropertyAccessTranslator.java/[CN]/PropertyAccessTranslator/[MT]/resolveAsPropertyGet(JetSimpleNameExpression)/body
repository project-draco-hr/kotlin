{
  JsName getterName=getPropertyGetterName(expression);
  if (getterName == null) {
    return null;
  }
  JsNameRef getterReference=TranslationUtils.generateCorrectReference(translationContext(),expression,getterName);
  return AstUtil.newInvocation(getterReference);
}
