{
  doAntTest(SUCCESSFUL);
  String outputFilePath=getOutputFileByName(JS_OUT_FILE).getAbsolutePath();
  RhinoUtils.runRhinoTest(Collections.singletonList(outputFilePath),new RhinoFunctionResultChecker("out","foo","box","OK"));
}
