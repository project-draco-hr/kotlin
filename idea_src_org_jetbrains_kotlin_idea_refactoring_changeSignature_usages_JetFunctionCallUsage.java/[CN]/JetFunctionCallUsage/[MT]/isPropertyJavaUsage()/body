{
  PsiElement calleeElement=this.callee.getElement();
  return (calleeElement instanceof JetProperty || calleeElement instanceof JetParameter) && resolvedCall != null && resolvedCall.getResultingDescriptor() instanceof JavaMethodDescriptor;
}
