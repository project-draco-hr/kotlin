{
  PsiElement calleeElement=this.callee.getElement();
  return (calleeElement instanceof KtProperty || calleeElement instanceof KtParameter) && resolvedCall != null && resolvedCall.getResultingDescriptor() instanceof JavaMethodDescriptor;
}
