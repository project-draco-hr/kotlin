{
  if (element instanceof PsiMethod && element instanceof PsiCompiledElement && ((PsiMethod)element).getContainingClass() instanceof JetLightClass) {
    return ((PsiCompiledElement)element).getMirror();
  }
  return super.substituteElementToRename(element,editor);
}
