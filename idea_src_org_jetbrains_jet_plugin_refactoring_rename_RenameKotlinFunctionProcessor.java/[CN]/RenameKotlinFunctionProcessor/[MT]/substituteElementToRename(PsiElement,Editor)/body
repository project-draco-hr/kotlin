{
  if (element instanceof PsiMethod && element instanceof PsiCompiledElement && ((PsiMethod)element).getContainingClass() instanceof KotlinLightClass) {
    return ((PsiCompiledElement)element).getMirror();
  }
  return super.substituteElementToRename(element,editor);
}
