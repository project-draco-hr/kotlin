{
  PsiMethod found=null;
  for (  PsiMethod method : methods) {
    if (match(method,fun)) {
      if (found != null) {
        throw new AssertionError("Duplicate for " + fun);
      }
      found=method;
    }
  }
  return found;
}
