{
  if (!at(LT))   return;
  PsiBuilder.Marker list=mark();
  advance();
  while (true) {
    parseModifierList();
    parseTypeRef();
    if (!at(COMMA))     break;
    advance();
  }
  expect(GT,"Expecting a '>'");
  list.done(TYPE_ARGUMENT_LIST);
}
