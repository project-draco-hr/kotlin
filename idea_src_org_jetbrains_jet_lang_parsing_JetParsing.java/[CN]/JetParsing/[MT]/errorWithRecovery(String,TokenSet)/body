{
  JetToken tt=tt();
  if (recoverySet == null || recoverySet.contains(tt) || (recoverySet.contains(EOL_OR_SEMICOLON) && (tt == SEMICOLON || myEOLInLastWhitespace))) {
    error(message);
  }
 else {
    errorAndAdvance(message);
  }
}
