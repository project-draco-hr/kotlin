{
  PsiBuilder.Marker list=mark();
  if (tt() == LT) {
    advance();
    while (true) {
      parseTypeParameter();
      if (!at(COMMA))       break;
      advance();
    }
    expect(GT,"Missing '>'",TYPE_PARAMETER_GT_FOLLOW);
  }
  list.done(TYPE_PARAMETER_LIST);
}
