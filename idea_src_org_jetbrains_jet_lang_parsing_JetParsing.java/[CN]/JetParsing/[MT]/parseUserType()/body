{
  if (at(NAMESPACE_KEYWORD)) {
    advance();
    expect(DOT,"Expecting '.'",TokenSet.create(IDENTIFIER));
  }
  while (true) {
    parseSimpleUserType();
    if (!at(DOT))     break;
    advance();
  }
}
