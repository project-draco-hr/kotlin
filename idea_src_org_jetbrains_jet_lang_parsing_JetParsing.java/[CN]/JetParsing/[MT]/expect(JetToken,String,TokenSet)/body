{
  JetToken tt=tt();
  if (tt == expectation) {
    advance();
    return true;
  }
  if (recoverySet == null || recoverySet.contains(tt)) {
    error(message);
  }
 else {
    errorAndAdvance(message);
  }
  return false;
}
