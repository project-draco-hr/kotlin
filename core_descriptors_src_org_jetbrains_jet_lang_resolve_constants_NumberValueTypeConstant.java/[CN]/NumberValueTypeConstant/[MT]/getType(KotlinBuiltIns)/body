{
  return new JetTypeImpl(Collections.<AnnotationDescriptor>emptyList(),getValue(),false,Collections.<TypeProjection>emptyList(),ErrorUtils.createErrorScope("Scope for number value type (" + getValue().toString() + ")",true));
}
