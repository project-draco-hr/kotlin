{
  KotlinType returnType=resolvedCall.getResultingDescriptor().getReturnType();
  CallInstruction instruction=new CallInstruction(valueElement,getCurrentScope(),resolvedCall,receiverValues,arguments,returnType != null && KotlinBuiltIns.isNothing(returnType) ? null : valueFactory);
  add(instruction);
  return instruction;
}
