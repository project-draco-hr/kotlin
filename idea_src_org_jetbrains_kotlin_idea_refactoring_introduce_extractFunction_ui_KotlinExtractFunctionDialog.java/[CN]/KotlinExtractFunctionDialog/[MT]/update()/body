{
  this.currentDescriptor=createDescriptor();
  setOKActionEnabled(checkNames());
  signaturePreviewField.setText(ExtractorUtilKt.getDeclarationText(getCurrentConfiguration(),false,IdeDescriptorRenderers.SOURCE_CODE_SHORT_NAMES_IN_TYPES));
}
