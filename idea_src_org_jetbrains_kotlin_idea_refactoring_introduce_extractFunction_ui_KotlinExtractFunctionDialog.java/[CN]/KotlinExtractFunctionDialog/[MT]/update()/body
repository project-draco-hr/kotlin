{
  this.currentDescriptor=createDescriptor();
  setOKActionEnabled(checkNames());
  signaturePreviewField.setText(ExtractionEnginePackage.getDeclarationText(getCurrentConfiguration(),false,IdeDescriptorRenderers.SOURCE_CODE_SHORT_NAMES_IN_TYPES));
}
