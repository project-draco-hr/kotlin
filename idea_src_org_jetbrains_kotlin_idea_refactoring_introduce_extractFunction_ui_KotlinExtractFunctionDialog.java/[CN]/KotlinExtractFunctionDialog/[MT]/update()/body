{
  this.currentDescriptor=createDescriptor();
  setOKActionEnabled(checkNames());
  signaturePreviewField.setText(ExtractionEnginePackage.getDeclarationText(currentDescriptor,getGeneratorOptions(),false,IdeDescriptorRenderers.SOURCE_CODE_SHORT_NAMES_IN_TYPES));
}
