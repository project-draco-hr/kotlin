{
  for (int i=0; i < yytext.length(); i++) {
    if (yytext.charAt(i) == '\n') {
      result.append("&nbsp;");
      result.append(addNewLineCloseTag());
      result.append(addNewLineOpenTag());
    }
  }
}
