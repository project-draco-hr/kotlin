{
  if (funDescriptor instanceof ConstructorDescriptor)   return funDescriptor.getContainingDeclaration().getName().asString();
 else   if (funDescriptor instanceof AnonymousFunctionDescriptor)   return "";
 else   return funDescriptor.getName().asString();
}
