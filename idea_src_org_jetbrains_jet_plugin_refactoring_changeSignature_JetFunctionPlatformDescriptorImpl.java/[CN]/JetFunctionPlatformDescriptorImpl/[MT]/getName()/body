{
  if (funDescriptor instanceof ConstructorDescriptor)   return funDescriptor.getContainingDeclaration().getName().getName();
 else   if (funDescriptor instanceof AnonymousFunctionDescriptor)   return "";
 else   return funDescriptor.getName().getName();
}
