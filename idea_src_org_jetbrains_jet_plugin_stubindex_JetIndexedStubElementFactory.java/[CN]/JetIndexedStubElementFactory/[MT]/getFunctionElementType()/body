{
  return new JetFunctionElementType("FUN"){
    @Override public void indexStub(    PsiJetFunctionStub stub,    IndexSink sink){
      String name=stub.getName();
      if (name != null) {
        sink.occurrence(JetIndexKeys.TOP_LEVEL_FUNCTION_SHORT_NAME_KEY,name);
      }
    }
  }
;
}
