{
  assert(startPosition != -1 && length >= 0 || startPosition == -1 && length == 0);
  this.source=source;
  this.sourceLine=line;
  this.sourceColumn=column;
  this.sourceStart=startPosition;
  this.sourceLength=length;
}
