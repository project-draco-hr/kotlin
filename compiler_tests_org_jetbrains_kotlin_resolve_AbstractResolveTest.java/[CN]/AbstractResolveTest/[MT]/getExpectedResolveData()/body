{
  Project project=getProject();
  return new ExpectedResolveData(JetExpectedResolveDataUtil.prepareDefaultNameToDescriptors(project,getEnvironment()),JetExpectedResolveDataUtil.prepareDefaultNameToDeclaration(project,getEnvironment())){
    @Override protected JetFile createJetFile(    String fileName,    String text){
      return createCheckAndReturnPsiFile(fileName,null,text);
    }
  }
;
}
