{
  DefaultModuleConfiguration.createStandardConfiguration(project,BuiltinsScopeExtensionMode.ALL).extendNamespaceScope(trace,namespaceDescriptor,namespaceMemberScope);
  if (hasPreanalyzedContextForTests()) {
    extendScopeWithPreAnalyzedContextForTests(namespaceDescriptor,namespaceMemberScope);
  }
}
