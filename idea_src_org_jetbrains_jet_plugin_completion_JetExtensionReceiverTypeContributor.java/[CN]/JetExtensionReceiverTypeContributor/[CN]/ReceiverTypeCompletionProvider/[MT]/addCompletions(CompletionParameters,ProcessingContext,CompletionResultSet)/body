{
  ResolveSession resolveSession=WholeProjectAnalyzerFacade.getLazyResolveSessionForFile((JetFile)parameters.getPosition().getContainingFile());
  JetCompletionResultSet jetCompletionResultSet=new JetCompletionResultSet(result,resolveSession,resolveSession.getBindingContext());
  if (parameters.getInvocationCount() > 0) {
    JetTypesCompletionHelper.addJetTypes(parameters,jetCompletionResultSet);
  }
  result.stopHere();
}
