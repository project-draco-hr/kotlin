{
  for (  JvmPrimitiveType type : JvmPrimitiveType.values()) {
    register(KotlinBuiltIns.BUILT_INS_PACKAGE_FQ_NAME.child(type.getPrimitiveType().getTypeName()).toUnsafe(),ClassId.topLevel(type.getWrapperFqName()));
  }
}
