{
  CharSequence chars=document.getCharsSequence();
  while (offset > 0) {
    offset--;
    char c=chars.charAt(offset);
    if (!Character.isWhitespace(c)) {
      return c == '.';
    }
  }
  return false;
}
