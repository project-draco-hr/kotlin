{
  Scopes newScopes=new Scopes(new JsScope(scopes.enclosingScope,"dummy enclosingScope for a block"),scopes.classScope,scopes.namespaceScope);
  return new TranslationContext(currentNamespace,program,bindingContext,newScopes);
}
