{
  FqName fqName=ResolveSessionUtils.safeFqNameForLazyResolve(psi);
  boolean isEnumEntry=psi instanceof JetEnumEntry;
  List<String> superNames=PsiUtilPackage.getSuperNames(psi);
  return new PsiJetClassStubImpl(getStubType(isEnumEntry),parentStub,fqName != null ? fqName.asString() : null,psi.getName(),superNames,psi.isTrait(),psi.isEnum(),isEnumEntry,psi.isAnnotation(),psi.isInner(),JetPsiUtil.isLocal(psi));
}
