{
switch (list.size()) {
case 0:
    return Collections.singletonList(toAdd);
case 1:
{
    List<T> result=new ArrayList<T>(2);
    result.add(list.get(0));
    result.add(toAdd);
    return result;
  }
default :
list.add(toAdd);
return list;
}
}
