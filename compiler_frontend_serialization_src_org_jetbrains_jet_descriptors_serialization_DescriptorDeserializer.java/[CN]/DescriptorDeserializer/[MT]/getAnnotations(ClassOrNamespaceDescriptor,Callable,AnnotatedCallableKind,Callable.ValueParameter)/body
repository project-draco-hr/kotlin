{
  return Flags.HAS_ANNOTATIONS.get(valueParameter.getFlags()) ? annotationDeserializer.loadValueParameterAnnotations(classOrNamespace,callable,nameResolver,kind,valueParameter) : Collections.<AnnotationDescriptor>emptyList();
}
