{
  Project project=getProject();
  InjectorForJavaSemanticServices injector=new InjectorForJavaSemanticServices(CompileCompilerDependenciesTest.compilerDependenciesForTests(CompilerSpecialMode.REGULAR),project);
  return injector.getPsiClassFinderForJvm().findPsiClass(new FqName(qualifiedName));
}
