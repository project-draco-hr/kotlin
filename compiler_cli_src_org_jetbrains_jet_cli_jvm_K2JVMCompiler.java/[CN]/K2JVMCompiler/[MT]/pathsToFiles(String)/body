{
  return ContainerUtil.map(Iterables.toArray(Splitter.on(File.pathSeparatorChar).split(paths),String.class),new Function<String,File>(){
    @Override public File fun(    String s){
      return new File(s);
    }
  }
,new File[0]);
}
