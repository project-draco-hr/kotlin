{
  String[] data=readData(kotlinClass,PACKAGE_FACADE);
  if (data != null) {
    return DescriptorsPackage.DeserializedPackageMemberScope(descriptor,JavaProtoBufUtil.readPackageDataFrom(data),context.getComponents(),new Function0<Collection<Name>>(){
      @Override public Collection<Name> invoke(){
        return Collections.emptyList();
      }
    }
);
  }
  return null;
}
