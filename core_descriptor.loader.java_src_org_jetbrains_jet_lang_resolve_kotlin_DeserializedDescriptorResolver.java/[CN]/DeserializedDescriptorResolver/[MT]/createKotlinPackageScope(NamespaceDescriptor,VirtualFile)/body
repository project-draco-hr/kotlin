{
  String[] data=readData(virtualFile);
  return data == null ? null : new DeserializedPackageMemberScope(storageManager,descriptor,annotationDeserializer,javaDescriptorFinder,JavaProtoBufUtil.readPackageDataFrom(data));
}
