{
  if (!elements.iterator().hasNext())   return Collections.emptyList();
  return CollectionsKt.map(elements,new Function1<Psi,Java>(){
    @Override public Java invoke(    Psi psi){
      return factory.create(psi);
    }
  }
);
}
