{
  JetCoreEnvironment environment=new JetCoreEnvironment(disposable,CompileCompilerDependenciesTest.compilerConfigurationForTests(configurationKind,true));
  environment.addToClasspath(getAnnotationsJar());
  return environment;
}
