{
  ModuleDescriptorImpl descriptor=new ModuleDescriptorImpl(Name.special(name),Collections.<ImportPath>emptyList(),PlatformToKotlinClassMap.EMPTY);
  descriptor.setModuleConfiguration(ModuleConfiguration.EMPTY);
  return descriptor;
}
