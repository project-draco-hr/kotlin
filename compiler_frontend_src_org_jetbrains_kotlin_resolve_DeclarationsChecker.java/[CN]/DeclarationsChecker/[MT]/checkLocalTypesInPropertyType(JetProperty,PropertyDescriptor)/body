{
  if (!isExposedAsPublicAPI(propertyDescriptor))   return;
  JetType propertyType=propertyDescriptor.getType();
  checkLocalTypesExposedInType(property,propertyDescriptor,propertyType,PROPERTY_TYPE_DEPENDS_ON_LOCAL_CLASS,new HashSet<JetType>());
}
