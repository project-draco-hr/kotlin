{
  if (descriptor.getContainingDeclaration() instanceof NamespaceDescriptor || descriptor.getVisibility() == Visibilities.PUBLIC || descriptor.getVisibility() == Visibilities.INTERNAL) {
    return ACC_PUBLIC;
  }
  return NO_FLAG_PACKAGE_PRIVATE;
}
