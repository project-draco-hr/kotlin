{
  if (descriptor instanceof PropertyAccessorDescriptor) {
    return isDeprecated(descriptor) ? ACC_DEPRECATED : getDeprecatedAccessFlag(((PropertyAccessorDescriptor)descriptor).getCorrespondingProperty());
  }
 else   if (isDeprecated(descriptor)) {
    return ACC_DEPRECATED;
  }
  return 0;
}
