{
  JetClassCompletionContributor.addClasses(parameters,result,session.expressionBindingContext,session.resolveSession,new Consumer<LookupElement>(){
    @Override public void consume(    @NotNull LookupElement element){
      addCompletionToResult(result,element,session);
    }
  }
);
}
