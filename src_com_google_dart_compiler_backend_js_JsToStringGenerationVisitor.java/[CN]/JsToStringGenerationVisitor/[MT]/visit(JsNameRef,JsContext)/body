{
  JsExpression q=x.getQualifier();
  if (q != null) {
    _parenPush(x,q,false);
    if (q instanceof JsNumberLiteral) {
      leftParen();
    }
    accept(q);
    if (q instanceof JsNumberLiteral) {
      rightParen();
    }
    parenPop(x,q,false);
    _dot();
  }
  _nameRef(x);
  return false;
}
