{
  JsExpression q=x.getQualifier();
  if (q != null) {
    parenPush(x,q,false);
    if (q instanceof JsNumberLiteral) {
      leftParen();
    }
    accept(q);
    if (q instanceof JsNumberLiteral) {
      rightParen();
    }
    parenPop(x,q,false);
    p.print('.');
  }
  _nameRef(x);
}
