{
  FunctionDescriptorImpl copy=new FunctionDescriptorImpl(newOwner,Lists.newArrayList(getAnnotations()),getName());
  copy.initialize(getReceiver().exists() ? getReceiver().getType() : null,DescriptorUtils.copyTypeParameters(copy,typeParameters),DescriptorUtils.copyValueParameters(copy,unsubstitutedValueParameters),unsubstitutedReturnType,DescriptorUtils.convertModality(modality,makeNonAbstract),visibility);
  return copy;
}
