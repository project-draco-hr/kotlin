{
  return computableWithProcessingCancel(new Computable<BindingContext>(){
    @Override public BindingContext compute(){
      return ResolveSessionUtils.resolveToElement(resolveSession,element);
    }
  }
);
}
