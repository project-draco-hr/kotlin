{
  JetType type=descriptor.getReturnType();
  return type != null && KotlinBuiltIns.isExactFunctionOrExtensionFunctionType(type) && !type.isNullable() && !InlineUtil.hasNoinlineAnnotation(descriptor);
}
