{
  return new ErrorReporter(){
    @Override public void reportIncompatibleAbiVersion(    int actualVersion){
      trace.record(ABI_VERSION_ERRORS,new AbiVersionErrorLocation(classFqName,file),actualVersion);
    }
  }
;
}
