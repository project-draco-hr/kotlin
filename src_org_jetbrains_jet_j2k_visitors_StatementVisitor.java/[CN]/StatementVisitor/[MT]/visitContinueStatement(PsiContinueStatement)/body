{
  super.visitContinueStatement(statement);
  if (statement.getLabelIdentifier() == null) {
    myResult=new ContinueStatement();
  }
 else {
    myResult=new ContinueStatement(Converter.$classobj.identifierToIdentifier(statement.getLabelIdentifier()));
  }
}
