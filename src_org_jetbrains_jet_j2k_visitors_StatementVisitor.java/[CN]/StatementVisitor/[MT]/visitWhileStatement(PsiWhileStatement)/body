{
  super.visitWhileStatement(statement);
  PsiExpression condition=statement.getCondition();
  @SuppressWarnings("ConstantConditions") Expression expression=condition != null && condition.getType() != null ? createSureCallOnlyForChain(condition,condition.getType()) : expressionToExpression(condition);
  myResult=new WhileStatement(expression,statementToStatement(statement.getBody()));
}
