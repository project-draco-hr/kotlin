{
  super.visitBreakStatement(statement);
  if (statement.getLabelIdentifier() == null) {
    myResult=new BreakStatement();
  }
 else {
    myResult=new BreakStatement(identifierToIdentifier(statement.getLabelIdentifier()));
  }
}
