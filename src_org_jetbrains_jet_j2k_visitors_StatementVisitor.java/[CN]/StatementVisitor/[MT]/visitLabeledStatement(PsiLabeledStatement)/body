{
  super.visitLabeledStatement(statement);
  myResult=new LabelStatement(Converter.$classobj.identifierToIdentifier(statement.getLabelIdentifier()),getConverter().statementToStatement(statement.getStatement()));
}
