{
  JetCoreEnvironment environment=new JetCoreEnvironment(getTestRootDisposable(),CompileCompilerDependenciesTest.compilerConfigurationForTests(configurationKind,mockJdk));
  environment.addToClasspath(JetTestUtils.getAnnotationsJar());
  return environment;
}
