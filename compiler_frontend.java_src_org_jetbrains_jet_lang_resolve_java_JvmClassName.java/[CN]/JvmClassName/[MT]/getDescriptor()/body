{
  if (descriptor == null) {
    StringBuilder sb=new StringBuilder(fqName.length() + 2);
    sb.append('L');
    sb.append(fqName.replace('.','/'));
    sb.append(';');
    descriptor=sb.toString();
  }
  return descriptor;
}
