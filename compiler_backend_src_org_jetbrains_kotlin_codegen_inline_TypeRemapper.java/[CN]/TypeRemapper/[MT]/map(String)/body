{
  String newType=typeMapping.get(type);
  if (newType != null) {
    return newType;
  }
  if (additionalMappings != null) {
    newType=additionalMappings.get(type);
    if (newType != null) {
      return newType;
    }
  }
  return type;
}
