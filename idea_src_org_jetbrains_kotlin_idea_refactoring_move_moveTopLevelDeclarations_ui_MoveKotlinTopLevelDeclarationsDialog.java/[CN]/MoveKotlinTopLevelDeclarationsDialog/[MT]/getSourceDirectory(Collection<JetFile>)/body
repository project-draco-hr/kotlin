{
  return CollectionsKt.single(CollectionsKt.distinct(CollectionsKt.map(sourceFiles,new Function1<JetFile,PsiDirectory>(){
    @Override public PsiDirectory invoke(    JetFile jetFile){
      return jetFile.getParent();
    }
  }
)));
}
