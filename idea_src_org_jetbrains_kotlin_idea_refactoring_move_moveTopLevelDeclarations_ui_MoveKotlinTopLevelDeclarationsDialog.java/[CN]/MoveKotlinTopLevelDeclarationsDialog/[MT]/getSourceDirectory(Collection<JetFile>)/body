{
  return KotlinPackage.single(KotlinPackage.distinct(KotlinPackage.map(sourceFiles,new Function1<JetFile,PsiDirectory>(){
    @Override public PsiDirectory invoke(    JetFile jetFile){
      return jetFile.getParent();
    }
  }
)));
}
