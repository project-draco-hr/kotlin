{
  return CollectionsKt.map(memberTable.getSelectedMemberInfos(),new Function1<KotlinMemberInfo,JetNamedDeclaration>(){
    @Override public JetNamedDeclaration invoke(    KotlinMemberInfo info){
      return info.getMember();
    }
  }
);
}
