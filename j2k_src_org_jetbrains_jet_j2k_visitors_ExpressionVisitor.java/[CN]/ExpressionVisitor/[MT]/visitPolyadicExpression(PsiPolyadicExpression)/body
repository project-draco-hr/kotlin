{
  super.visitPolyadicExpression(expression);
  myResult=new PolyadicExpression(getConverter().expressionsToExpressionList(expression.getOperands()),getOperatorString(expression.getOperationTokenType()),getConverter().createConversions(expression,PsiType.BOOLEAN));
}
