{
  super.visitInstanceOfExpression(expression);
  myResult=new IsOperator(getConverter().expressionToExpression(expression.getOperand()),getConverter().elementToElement(expression.getCheckType()));
}
