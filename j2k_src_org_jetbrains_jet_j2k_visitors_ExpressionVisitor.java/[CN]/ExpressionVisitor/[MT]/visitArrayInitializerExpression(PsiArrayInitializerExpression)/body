{
  super.visitArrayInitializerExpression(expression);
  myResult=new ArrayInitializerExpression(getConverter().typeToType(expression.getType()),getConverter().expressionsToExpressionList(expression.getInitializers()));
}
