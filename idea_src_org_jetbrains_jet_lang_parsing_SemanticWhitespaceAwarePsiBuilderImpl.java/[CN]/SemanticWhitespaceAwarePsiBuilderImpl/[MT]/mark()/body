{
  return new MarkerAdapter(super.mark()){
    private final boolean eolInLastWhitespace=eolInLastWhitespace();
    @Override public void rollbackTo(){
      super.rollbackTo();
      myEOLInLastWhitespace=eolInLastWhitespace;
    }
  }
;
}
