{
  List<TypeProjection> arguments=getArguments(classDescriptor);
  return new JetTypeImpl(Collections.<Attribute>emptyList(),classDescriptor.getTypeConstructor(),false,arguments,unsubstitutedMemberScope);
}
