{
synchronized (lock) {
    EntryInfo info=convertToISEntry(file);
    if (info == null) {
      return new byte[0];
    }
    byte[] content=info.content;
    if (content == null) {
      return new byte[0];
    }
    return content;
  }
}
