{
  if (inputStream == null) {
    try {
      inputStream=new VirtualJarFile(myFileSystem,path).getInputStream();
    }
 catch (    IOException e) {
      e.printStackTrace();
    }
  }
  myBasePath=path;
}
